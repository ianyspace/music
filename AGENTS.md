# music — 项目约定

独立出来的音乐播放器（Next.js 15 Pages Router + Sass），**仓库根目录即项目**，
构建为静态导出后发布到 GitHub Pages 项目页：https://ianyspace.github.io/music/

代码从博客仓库 `space` 的 `components/Music/` + `pages/music/` 整体切出，
播放逻辑一行没改，只替换了三个博客专有的依赖（见下方「迁移时替换了什么」）。

**`/h5`、`/desktop` 是两棵完全独立的组件树**：彼此不 import 任何一个文件，
只共用 `components/Music/core/`（状态机 + 数据层 + 面板内容）和它旁边的纯函数。
桌面端的每一个面都是**纯 CSS 毛玻璃**（`backdrop-filter` + `--glass-*` token）。
详见下方「两套布局」。

## 目录

| 路径 | 作用 |
| --- | --- |
| `pages/index.js` | 入口路由，按屏宽决定跳 `/h5` 还是 `/desktop` |
| `pages/h5.js` / `pages/desktop.js` | 两个薄路由，各自只渲染一棵树，互不相干 |
| `components/Music/h5/` | 手机端：`MusicApp` 外壳 + 一个页面（列表）+ 迷你条 + 播放页 + 五个 `SheetChrome` 面板（账号 / 听歌排行 / 音乐库 / 缓存 / 云盘）+ 两个底部抽屉（⋮ / 行抽屉） |
| `components/Music/desktop/` | 宽屏端：`DesktopApp` 外壳 + 沉浸式舞台（`DesktopMusic`）+ 玻璃弹窗 + 行抽屉 |
| `components/Music/core/` | 两套布局共用的中性核心（见下方「两套布局」） |
| `components/Music/`（根） | 只放共享件：`Cover` / `Marquee` / `icons` / `shared` / `audioCache` / `librarySource` / `playStats` / `likes` |
| `cloudflare-worker/schema.sql` | D1 的建表语句（`plays` + `likes`；Worker 自己也会跑一遍同样的 DDL） |
| `lib/cache/indexedDb.js` | IndexedDB 薄封装（缓存存储层） |
| `public/sw.js` | 离线外壳的 service worker：网络优先、失败才回缓存，只缓存本站的 HTML / CSS / JS / 图标 |
| `utils/registerServiceWorker.js` | 注册它。只在生产构建里注册，`npm run dev` 下不注册 |
| `public/` | 站点图标（favicon.ico + PNG 一套）+ `sw.js`，构建时原样拷进 `out/` |
| `cloudflare-worker/` | Cloudflare Worker，把 R2 桶暴露成曲库清单 |
| `scripts/preview-*.js` | 视觉核验：读**构建产物里的真实 CSS** + 硬编码 markup 生成单文件 HTML，用浏览器打开即可量尺寸 |
| `styles/index.scss` | 唯一全局样式入口，只由 `pages/_app.js` 导入 |

> **`public/` 里只有图标和 `sw.js`** —— 离线外壳回来了，但和上一代是两回事（网络优先、只做兜底），见下方。
> **`utils/basePath.js` 已删除**：`withBasePath()` 的唯一调用者是原 `_app.js` 里的 SW 注册，
> SW 一走它就没有使用者了。但**图标的 basePath 得自己拼**（见下一条）。

## 关键约定

- **样式**：全局样式**只能**由 `pages/_app.js` 导入（Next pages router 限制）；
  组件样式与组件同目录 `Foo.module.scss` + `import styles from './Foo.module.scss'`；
  kebab-case 类名必须写 `styles['foo-bar']`。
  CSS Modules 生成的类名是 `[文件名]__[类名]__[hash]`，**目录不进名字** ——
  所以把文件挪进子目录不会改类名（这次拆分正是靠这一点）。
- **两棵树不许互相 import**：`components/Music/h5/**` 里不许出现 `desktop`，其余同理。
  共用的东西只能落在 `components/Music/core/` 或 `components/Music/` 根下
  （根下的 `icons.js` 是共享图标集）。
  这条没有脚本兜底（见「检查脚本已删除」），改完请自己 `grep` 一遍。
- **basePath**：`config/index.js` 的 `site.pathPrefix = '/music'` 是唯一来源（`next.config.js` 读它）。
  `next/link`、`next/image`、`_next/*` 会自动带上；**`public/` 里的文件不会**，所以手写
  `<img src="/favicon.ico">` 会 404 —— 走 `shared.js` 的 `assetUrl(path)`（它 import `config`
  拼前缀）。原来的 `utils/basePath.js` 随 SW 一起删了，`assetUrl` 是它唯一长回来的那部分。
- **全屏浮层别放进被 `transform` 的子树**（重要，踩过坑）：手机端 `.view-in` 的 tab 切换动画
  会让 `transform` 保留终态，而带 `transform` 的祖先会成为 `position: fixed` 后代的包含块，
  于是 `inset: 0` 撑成整个滚动高度、面板被推到最底部（表现为"只有遮罩没有抽屉"）。
  抽屉 / 缓存管理这类全屏浮层一律挂在各自外壳（`h5/MusicApp` / `desktop/DesktopApp`）
  的最外层渲染 —— 桌面端虽然没有 tab 动画，也照此办理，免得以后加了动画再踩一次。
- **有一个 Service Worker，但它只做断网兜底、不做加速 —— 往里加东西之前先读这一段**。
  这个仓库删过一次 worker：它**预缓存**外壳，代价是部署后旧外壳继续吐旧 JS，
  而且「我现在看到的是不是最新版」没法靠刷新回答 —— 排查线上问题时这个不确定性反复误导过判断。
  `public/sw.js` 堵死这条的方式是**网络优先**：所有请求先 `fetch()`，只有它抛错（断网）才回缓存。
  于是「有 worker」不再等于「可能在读旧的」—— 在线拿到的永远是线上那一份，刷新一次就能确定版本。
  **这条策略是这个文件存在的全部理由；改成 cache-first 就等于把那两个代价一起请回来。**
  - **缓存范围只有本站的 HTML / CSS / JS / 图标**。曲库 Worker、R2 音频、封面都是跨域的，
    一律不碰（跨域响应要么不透明、要么带 `Range`，都不适合整份缓存）。
    音频和清单归应用自己的 IndexedDB / localStorage 管，见上面那两条。
  - **安装时除了三条路由和图标，还会顺着 HTML 把里面的 `_next/static/**` 抓一遍。**
    这一遍不能省：安装发生在第一次访问时，而**那次访问的请求不经过这个 worker**（它还没接管），
    不补这一遍的话「访问过一次然后断网」是打不开的 —— 外壳在、脚本不在。
  - **`activate` 里的清理按前缀**（自己的 `music-offline-` + 上一代的 `music-shell-` /
    `music-runtime-`）：`caches` 是整个 origin 共用的，而这个 origin 上还挂着博客（`/space/`），
    不能顺手删别人的缓存。原来的 `utils/retireServiceWorker.js` 已经删掉 ——
    它注销的是 origin 上**所有**注册（会把博客的 worker 一起干掉），而旧注册现在会被
    同一个地址上的新脚本顶掉，旧缓存由 `activate` 清。
  - **注册只在生产构建里发生**（`utils/registerServiceWorker.js`）：`npm run dev` 下注册会把
    dev server 的请求也拦下来，改代码看不到效果。本地要看离线行为，用 `npm run build` +
    `scripts/serve-static.js` 服务 `out/`（那也是产物本身的形态）。
  - **它不影响冒烟脚本**：`drive-page.js` 打的是真网络，而 worker 是网络优先。
    但万一遇到「明明改了却看不到」的怪事，先怀疑它 —— devtools 的
    Application → Service Workers 里 Unregister 就能排除。
  - **回归检查是 `scripts/probe-offline.js`**（`npm run build` 之后直接跑，自带静态服务器）。
    它守的就是上面这几条：接管、缓存清单、**在线不读缓存**、**断网真的回缓存**、清理按前缀。
    **把 worker 改成 cache-first 的话它会红** —— 那是这个文件唯一不能碰的约定。
    它模拟断网的方式是**关掉自己的静态服务器**，不是 CDP 的网络仿真：要断的是 worker 自己的
    `fetch()`，而仿真不一定盖得到它（踩过：用仿真跑，断网那一步和在线那一步读到的是同一个
    结果，看着像通过，其实什么都没证明）。
- **一行文字别用 flex 包**（踩过坑）：`Marquee` 里的 `.item` 原本是 `inline-flex; align-items: center`，
  而 flex 容器会把每个子文本块 blockify —— 于是「行首」落在每个子块自己的开头，
  CSS 会把**行首那个可折叠空格**吃掉。结果 `title - artist` 里 `" - "` 只剩右边那个空格，
  渲染成 `歌名- 歌手`：左边 0px、右边 4.3px（`drive-page.js` 现在量这两个数，
  差别超过 0.6px 就红）。**空格是标记里写的东西，就该由标记决定** —— `.item` 现在是
  `display: block`，子块保持 inline，`" - "` 原样出现。`text-align: left` 是配套的：
  居中变体（播放页顶栏那一行）的祖先设了 `text-align: center`，而 flex 容器根本不看这个值，
  一块窄于自己内容的文字必须还是往右溢，不能两头都冒出去。
- **`Marquee` 里有两个宽度，问「放不放得下」只能用 `ink` 那个**（踩过坑）：
  `inner.offsetWidth` 是 `advance` —— 一个循环走多远，也就是**标签 + 折返处那个间距**
  （`.item` 自己的 padding）。它只该拿去算动画时长。而「放不放得下」只跟字形有关，
  所以要用 `Range.selectNodeContents(inner).getBoundingClientRect().width` 量 `ink`。
  拿 `advance` 去比，判断式就变成了 `ink + 40 > slot`，**两个方向都错**：
  一个还空 20px 的标签照样滚动（`童年收（cover：F.Be.I音乐团队）` 实测 281.7px 的字
  在 302px 的槽里，在滚），反过来**溢出不到 40px 的标签被判成放得下 —— 直接裁掉，
  既没有滚动也没有渐隐**，后者更糟，因为屏幕上没有任何东西说后面还有字。
  居中变体左边也带 padding，所以它算的是 80。
- **槽宽变了要重新问一次，`ResizeObserver` 就是干这个的**：effect 只在标签换了的时候重跑，
  所以旋转、改窗口大小、旁边那个邻居变长把这一行挤窄 —— 这些都不会触发重新测量，
  于是「刚开始溢出」的那个标签会一直按旧宽度的结论被裁着。`measure()` 只写类名和动画时长，
  两个都在它自己观测的那个盒子里，所以不会自激。
- **歌词样式：挂在「换行」上就够了**（`h5/NowPlaying.js` 的 `LYRIC_OPTIONS`，
  两个样式：普通 / 沉浸单行）。沉浸单行 = 只留当前行和上下各一行，当前行从更小的尺寸长起来。
  它是**靠换行触发、纯 CSS** 的，而这是这个页面最省力的地方：换行时组件本来就会重渲一次，
  而 CSS 动画在属性第一次生效的那个元素上自动开始 —— 类名每次都落到不同的元素上，
  所以动画正好每行跑一次。没有时钟、没有定时器、没有需要手动重启的东西。
- **要让当前行「长大」，动 `transform` 不动 `font-size`**：当前行 21px、相邻行 16px，
  所以动画 `font-size` 会让整个列表在动画的每一帧重排，把上下每一行都推来推去。
  `scale()` 画出同样的长大而不碰布局。
- **缓动曲线要量，不要猜**：第一版用的是强减速的 `cubic-bezier(0.2, 0.65, 0.25, 1)`，
  读起来很「高级」，量出来是**把 72% 的成长塞进了 420ms 里的前 120ms**
  （钉住 `currentTime`，读绘制出来的盒子：38 → 46 → 49 → 50px 高），
  后 320ms 移动不到一个像素 —— 眼睛拿到的是「啪一下变大」再等一会儿，不是「长起来」。
  换成对称的 `cubic-bezier(0.4, 0, 0.2, 1)`，同样的时长，变化铺满全程。
  **这类判断只有读数/截图能给**：两条曲线在代码里看起来一样对。
- **沉浸单行靠 `lyrics.timed` 兜底**：它跟的是时钟，没有时间戳就没有时钟可跟，退回 `plain`。
  不这么做的话它会「只剩最后一行」—— 无时间戳时每行的 `time` 都是 0，
  那个 reduce 会落在最后一行上。
- **覆盖变体规则时，选择器要原样重复**：沉浸单行的开关是 `.lyrics-solo .lyric-active`（两个类），
  所以 `prefers-reduced-motion` 里覆盖它必须也写两个类 —— 写一个类的
  `.lyric-active { animation: none }` 无论放多后面都会输给它（特异性 0-2-0 > 0-1-0）。
  第一次就是这么写的：缩进对、位置对、规则完全不生效。
- **CSS Modules 里不要用 `styles['前缀-' + 名字]` 拼类名**：名字不在样式表里时返回 `undefined`，
  模板字符串会把它变成字面量字符串 `undefined` 挂到元素上 —— 一个静默匹配不到任何东西的类名。
  偏偏在这里「什么都没匹配到」也是正确答案，所以永远发现不了。用模块级的映射表
  （`LYRIC_STYLE_CLASS`），拼错的代价是构建期就看得见的一个 `undefined`。
- **单选组给了 roving `tabIndex`，就必须自己处理方向键**：两个样式行是
  `role="radiogroup"` + `role="radio"`，只有被选中的那一行 `tabIndex=0`。这是「一组一个 Tab 停靠点」
  的正确做法，但**同时**把另一行从 Tab 顺序里拿掉了 —— 不写 `handleStyleKeys` 的话，
  键盘用户永远够不到它，而 `role="radio"` 本身就是在承诺方向键可用。
- **删掉一个已存的选项时，白名单就是迁移方案**：`LYRIC_STYLES` 从四个名字减到两个之后，
  之前选过那两个的访客手上还留着那个名字 —— 恢复时那句 `LYRIC_STYLES.includes(saved)`
  是他们和「一个没有任何 CSS 在背后的样式名」之间唯一的东西，落到默认的普通。
  **不用清 localStorage，也不用换 key**：把名字从白名单里拿掉就是全部。
  这条有断言（`drive-page.js` 往 localStorage 里塞 `wipe` 再刷新，看抽屉落在哪一行），
  因为它在 UI 上根本走不到 —— 「走不到」正是删掉它的含义。
- **逐行上浮 和 卡拉OK 扫光 是主动砍掉的，别再提**：扫光是唯一自己开 rAF 的样式
  （每秒六十次写 `--wipe` 去推一个渐变断点），逐行上浮是那个从下方升起的动画。
  两个都不在 `LYRIC_STYLES` 里了；要恢复它们，先想清楚要不要把那个 rAF 一起带回来。
- **一次「某个效果贵不贵」的测量，能留下的是方法，不是结论**（`probe-wipe-cost.mjs`；
  它量的是当时那个扫光样式，样式已经砍了，但下面每一条都还成立）：
  - **每个格子至少跑三次**。同一个格子三次的 `longtask` 是 220 / 366 / 56 毫秒，
    连「关歌词只放唱片」那一档自己都是 106 / 326 / 510 —— 它数的是「有没有一个 ≥50 ms 的长任务
    恰好落进这个窗口」，一个 271 ms 的离群样本就能把总数抬到 700 以上。
    第一遍只跑一次，得出的「A 是 B 的三倍」纯粹是噪声。
  - **别拿另一个场景当基线**。当时拿「关歌词、只放唱片」当底，但那是另一张一直在转的唱片：
    1× 下它和歌词页打平，4× 下它反而**最慢**（31.0–31.8 fps，歌词两档都在 34 以上）。
    要问「X 比 Y 贵多少」，只能拿同一场景里只差 X 的那两个比。
  - **fps 的区间重叠时，还能分开的是累计计数器**：`RecalcStyleDuration` / `ScriptDuration`
    能说出多花的时间花在哪一类工作上 —— 当时那个样式多花的是**样式重算**（每秒六十次写一个
    自定义属性，约 0.3 ms 一帧、一帧的 1.5%），而 `ScriptDuration` 三档都一样，
    也就是「rAF 回调本身几乎免费」。`RecalcStyleCount` 三档都是 276：**次数没变、单次变贵**。
  - `Performance.getMetrics` 在 `Performance.enable` 之前全是 0。
  - 这些绝对值不是手机的：headless Chrome、软件光栅、没有 GPU，而且这个环境的音频管线
    根本不渲染。可比的是同一张表里行与行之间的关系。
- **图标**：源文件是 `1000x1000` 透明底 PNG（圆形色层渐变），已导出成 `public/` 下四个文件。
  `favicon.ico` 是**真的多尺寸 ICO**（16/32/48 三档，PNG 载荷），不是改名的 PNG ——
  有些工具产出的「ico」其实只是把 PNG 改了扩展名，Windows 上会显示不出来。
  重做时用 `sharp` 出各尺寸 PNG，再手拼 ICO 容器（见提交 `167dae8` 之后那次的处理方式）。
- **图标的 basePath 必须自己拼**（踩过坑）：`public/**` 是原样拷进 `out/` 的，
  Next **不会**像 `next/link` / `_next/*` 那样替你加前缀。写成 `/favicon.ico` 在项目页
  （`/music/`）上会指向用户站点根目录然后 404 —— 本站在补上这个之前，favicon 一直是坏的。
  `pages/_document.js` 里用 `site.pathPrefix` 拼，CI 也断言这三个图标文件存在。
- **缓存**：音频 blob 存 IndexedDB（`lib/cache/indexedDb.js`），key 是 `<source>:<track id>`；
  曲库清单存 localStorage。**两者寿命不同，别搞混**：
  - **音频 blob 有 30 天 TTL**（`audioCache.js` 的 `CACHE_TTL_MS`），**每播一次就续期到 30 天后**，
    30 天没播放过才清除。最后一次播放时间存在 localStorage 的 `music:cachePlayed` 里
    （`expiryOf()` = 上次播放时间 + TTL，没打过戳的记录回落到保存时间）。
  - **曲库清单永久**（`librarySource.js` 的 `expiresAt: NEVER_EXPIRES`）——清单只是一个 URL 数组，
    过期了只会让人看到空列表，没有任何收益。
  - `NEVER_EXPIRES = 0` 的语义是「**没有任何东西给这条记录打时间戳**」，不是「永久」。
    `expiryOf()` 对没有戳的记录返回 `NEVER_EXPIRES`：**「不知道它什么时候播过」不能当成
    「它很旧」的证据**，否则会把访客整个缓存删掉。
  - 旧版本写入的带真实 `expiresAt` 的记录仍按原时间生效，不会被意外「永久化」。
- **缓存的读取路径绝对不能写**（踩过坑，且症状极难定位）：第一版 TTL 是在读缓存时把新过期时间
  写回同一条记录，而 IndexedDB 的 `readwrite` 事务会**串行化阻塞同一个 store 上的所有其他事务** ——
  于是「读下一首的缓存」永远等不到，表现为**点了播放没反应**（`6b3cd85` / `7f68c72`）。
  续期只能在播放真正开始之后做。（原来有个 `check-cache-ttl.js` 守着这条，已随检查脚本一起删，
  改 `audioCache.js` 时请自己确认「读路径一行都没写」。）
- **`audioCache.js` 上的函数全部是 best-effort**，IndexedDB 不可用时自动退化成纯联网播放。
  这层是**唯一**的离线能力，所以它坏掉时症状是「播放列表空 / 不缓存」而不是「网站打不开」。
- **改 IndexedDB 的 `keyPath` 或建索引，必须同时升 `DB_VERSION`**：
  store 已存在时 `createObjectStore` 是空操作，不升版本号只有新访客能拿到修复。
  另外 `styles.x` 写错只返回 `undefined`、类名被静默丢掉，元素照常渲染却毫无样式 ——
  `npm run build` **不会**报这个（原来靠 `scripts/check-css-modules.js` 兜住，已删）。
  现在只能靠人眼：新增/改名类名时，`styles.foo` 里的 `foo` 一定要在同一个
  `Foo.module.scss`（或它 `composes` 的 `sheetBase.module.scss`）里出现过。
- **封面和歌词都是「同名附属文件」**：数据层给两种来源各留一组字段（cloud 是 Worker 解析好的
  公开链接 `coverUrl` / `lyricsUrl`，drive 是文件对象 `coverFile` / `lyricFile`），
  **只有 `coverUrlOf()` / `lyricsUrlOf()` 知道哪个是哪个**，调用方一律走它们，不要自己判 source。
  渲染时封面是**盖在渐变之上的叠加层**（`components/Music/Cover.js`）：有图用图，
  没有、或图加载失败（歌没传封面、Drive 缩略图过期、Worker 还是旧版）就露出调用方本来就画着的
  `trackGradient` —— 所以「没有封面」不需要任何单独的分支，`Cover` 返回 `null` 就是全部处理。
  两点容易踩：① 图片是 `position: absolute` 的叠加层，宿主 tile 必须是定位元素，且 `<Cover>`
  要放在**第一个子节点**（音符图标被它盖住是对的，行的播放/暂停遮罩必须盖在它上面）；
  ② **公共曲库加封面要重新部署 Worker** 才生效，旧 Worker 不返回 `coverUrl` 时客户端会退回
  按 `.jpg` 猜名字（`guessCoverUrl`，代价是每首没封面的歌一个 404）。
  **锁屏 / 耳机键的封面是第三个用到它的地方**（`core/usePlayer.js` 的 mediaSession effect，选图逻辑在
  `shared.js` 的 `mediaArtwork`）。这里没有 `Cover` 那种「返回 null 就露出渐变」的便宜：
  那张图是**操作系统自己去取的**，取不到时只会空白，所以失败要由我们兜 —— 一个 `Image()`
  探针的 `onerror` 会**再写一次 metadata**，把 `mediaArtwork('', name)` 画出来的渐变发过去。
  探针必须在 effect 的清理里摘掉，否则上一首的失败会重画下一首的 metadata。
  `MediaMetadata.artwork` 里只有渐变那一项声明 `sizes`（它按构造就是 320×320），封面不声明 ——
  不知道尺寸的 `sizes` 是给锁屏的一句假承诺。
- **空列表的文案只有一处决策**（`shared.js` 的 `emptyListMessage`），四个分支按顺序判断：
  还在加载 → 搜了没搜到 → 「只看喜欢」开着 → 曲库真的空。三个容易搞错的地方：
  - **它不需要知道曲库有多大**。这里以前传一个 `libraryCount`（过滤前的 `tracks.length`），
    用来分辨「曲库有歌，但都被移进『不喜欢』了」。那类偏好删掉之后，**没有任何机制能让歌
    凭空消失** —— 屏幕为空就是曲库为空（或被搜索 / 只看喜欢收窄到空），所以那个分支和参数
    一起没了。要再加回「曲库有歌却一行都没有」的说法，先想清楚是谁把行拿走了。
  - **`likedOnly` 也要传进来**（只有手机端有），否则空的我喜欢会被说成「这里没有音频文件」。
    关键词那一支即使开着「只看喜欢」也先说关键词 —— 访客打了字，他要听的就是那件事 ——
    但会说清是在哪儿找的（「喜欢的歌曲里没有匹配…」），因为「没搜到」而过滤器悄悄开着是最费解的说法。
  - **文案是列表的兄弟节点，不能塞进 `<ul>`**：`<ul>` 里只能有 `<li>`，塞 `<p>` 是无效 HTML，
    而且读屏会把这句话当成列表的一项念出来。手机端列表**保持挂载**（播放条的「回到正在播放」
    按 `id="ms-track-list"` 找它），所以消息挂在 `<ul>` 之后；桌面端则是直接把列表换掉。
    两端共用 `.list-empty` 这一个类名（以前手机端叫 `lib-loading` / `lib-empty`，还共用一条规则）。
  文案由 `node scripts/preview-empty-list.js` 出图核对。
- **公共曲库的默认顺序 = 清单的顺序，由 Worker 按「加进来的时间」倒序排**（新的在最上）。
  前端**没有**任何排序：`visibleTracks` 只有 `applyPinnedOrder`（只把置顶那几首挪上去）→
  只看喜欢 → 搜索，没有一条会重排剩下的。所以这件事只在
  `cloudflare-worker/src/index.js` 的 `byNewestFirst` 一处决定 ——
  **改它要单独部署 Worker**（`cd cloudflare-worker && npx wrangler deploy`），
  它**不随站点推送上线**（`cloudflare-worker/**` 在 `paths-ignore` 里）。
  - **为什么不在前端排**：前端只能按**响应里带的字段**排，而边缘缓存会在部署后继续发
    最多 `CACHE_TTL_SECONDS`（5 分钟）的旧响应 —— 那几分钟里整个清单都缺这个字段，
    列表会毫无理由地自己重排一遍。排在 Worker 里，旧缓存只是「还是旧顺序」五分钟。
  - **同一毫秒的（一次批量上传就是这样）按文件名排，别把这条兜底删掉**：桶的列举顺序
    不是谁选的，`Array#sort` 稳定只保证「从输入可复现」，不保证输入本来有顺序。
    排序放在 `map` **之前**，所以 `uploaded` 是列举的细节，不会跑进线上格式。
  - Drive 那份不在这里排：它请求时就带了 `orderBy: 'name'`，顺序是 Drive 给的。
  - 查证过的事实（2026-09-22）：R2 确实为每个对象记着写入时间 —— 公开域名上 `HEAD` 一个
    对象能读到 `Last-Modified`，45 首里 44 首有值（剩下那首是早就删掉的 404），时间互不
    相同，但**成批聚集在几秒之内**，这正是兜底那条要存在的理由。列举结果里的 `uploaded`
    和它是同一个值（类型里不是可选的）。另外：**这台机器连不上 `*.workers.dev`**
    （本地 DNS 把它解析到一个连不通的地址，DoH 也不通），所以「线上真的变了」这一步
    只能由人刷新页面确认，别在这里反复试。
- **列表偏好现在只剩一个在本机：置顶排名**（`shared.js` 的 `ORDER_KEY`）。它是
  `<source>:<id>` 的 JSON 数组，走 `readKeyList` / `writeKeyList`，在 `localStorage`。
  它和「只看喜欢」一起进 `visibleTracks`，顺序是：`applyPinnedOrder`（只排序）→ 只看喜欢 → 搜索。
  喜欢必须在搜索**之前**，因为搜索是对「已决定显示什么」的再收窄。
  **两个都只排序或收窄，没有一个会删行** —— 这正是空列表文案能少一个分支的原因。
  只做视觉过滤（放在列表组件里）会让角标数量、上一首/下一首和屏幕上的行对不上。
  只有公共曲库能喜欢 —— `toggleLike` 拒绝 `drive:` 的键（文件 id 出了那个账号就没有意义，
  存下来是一个永远匹配不上的键），三处 UI 也不渲染。所以 `liked` 里实际上都是 `cloud:`。
  `likedOnly` 本身**不持久化**：它是「列表现在在显示什么」，和搜索框一类，不是访客的决定。
- **「喜欢」搬去了 D1，而且是按 QQ 分的**（`components/Music/likes.js`，见「我喜欢（D1）」一节）。
  它以前是 `LIKED_KEY` 那份本机数组，和 `order` 同形；现在是 `GET/POST /likes`。
  搬家的理由是它本来就属于「这个人」而不是「这台设备」——和播放次数同一个理由。
  **但没确认 QQ 也能喜欢**：没有号码时喜欢落在本机的 `guest` 桶里，一个字节都不出网
  （`applyLikeChange` 对 guest 直接 `return`，连 outbox 都不写）。
  这里曾经写的是「没有绑定 QQ 就没有喜欢」，三处 UI 在没绑号时都不渲染 —— 那等于把一个
  功能藏到访客先完成另一件事之后，而喜欢本身并不需要那个号码才成立。
  确认号码时 `adoptGuestLikes` 把本机这些喜欢交给这个号（进 outbox，随队上传）再清空 guest 桶 ——
  见「我喜欢（D1）」。
- **置顶是可逆的，而「取消置顶 = 恢复默认排序」不需要任何新数据**。`order` 是**排名**而不是全排列，
  只存访客置顶过的 key；没有 entry 的歌在 `applyPinnedOrder` 的稳定部分排序里就落在源顺序 ——
  所以 `unpinTrack` 只是把 key 从数组里删掉，忘掉**就是**恢复。别再存一份「原来的位置」。
  抽屉的状态来自 `isPinned`（在不在这个排名里），**不是**「是不是在第一行」：搜索或只看喜欢
  会把置顶的歌推下去，两者就不再是同一个问题了。抽屉曾经按第一行判断，于是把自己 `disabled` 掉，
  变成只能置顶、不能取消 —— 而 `置顶` / `取消置顶` 是**一个按钮两种文案**（`togglePin`），
  和 `喜欢` 同形；`pinTrack` / `unpinTrack` 留在 hook 内部，两套布局都不许自己判断。
- **手机端顶部栏：应用图标在最左，搜索 / ❤ / ⋮ 在最右**（`h5/TrackList.js` 的 `<header>`）。
  品牌名和标题被拿掉了，位置给了一个 **40px 的圆角方块**（`MarkNote`，样式在
  `core/MarkNote.module.scss` 的 `.mark`），它同时是**进入「账号」抽屉页的按钮**。
  **它现在住在 `core/`，因为两套布局都画它**：手机端在列表栏最左，桌面端在列表列最左，
  同一个组件、同一份印谱 —— 两棵树各画一个 logo 就是两个 logo。桌面端唯一改的是盒子
  （`side-mark`：36px、去掉 6px 内缩、和列左边缘齐平，因为那一行是 36px 的方块行），
  **尺寸变了波纹不会跑**：里面每一层都是这个盒子的百分比，`WAVE_ORIGIN` 的推导里那个
  40px 只是把算术写具体，结论与尺寸无关。`className` 是调用方唯一能改的东西 ——
  **除了盒子，别的都别碰**。
  下面这些约定是这套组件自己的，两套布局都适用：
  - **它在最左边，因为那里原来就是曲库名的地方**。中间有一版把它挪到了最右（理由是
    「列表的左边缘是内容开始的地方，一个要离开列表的控件不该是眼睛落下的第一件事」），
    后来又挪回来了：**左图标、右操作是手机应用本来的样子**，而「它通向设置」这个顾虑
    已经被右下角那个状态点抵消 —— 它现在一眼回答「我的号存上了没有」，那是列表页该说的话。
  - **`margin-left: 6px` 是为了和下面的封面竖直对齐**，不是随手挪的：一行是 44px 缩略图、
    内缩 4px，所在的列本身再内缩 18px，所以封面的中心离页面左边缘 44px；而 40px 的图标
    贴着列边放，中心在 38px。6 正好是这个差值。**图标不是一行，它只是要和那些行对「这一列
    从哪开始」这件事达成一致**。
  - **`margin-left: auto` 现在只是保险**：这一行的另一个子节点是 `sr-only` 的 `<h1>`，
    而它是 `position: absolute`，**根本不是 flex item**。图标回到流内之后，`space-between`
    重新有了两个真实边缘可以推开，所以它不再是承重的；留着是为了让「这一组在行尾」属于
    这一组自己，而不是依赖这一行恰好有几个子节点。
  - **它是圆角方块，不是圆**。画的是应用图标，圆形让它读成头像 —— 那正是这个按钮
    以前的样子（也正因为那样，头像搬去了「账号」页的身份卡）。圆角只写在按钮上
    （`border-radius: 11px`），里面那层 `.clip` 用 `border-radius: inherit` 跟上，
    所以底图的圆角不会和按钮走散。
  - **QQ 状态是右下角的一个小圆点**：未确认灰（`#8e8e93`）、已确认绿（`#34c759`），
    9px，外面一圈 1.5px 的 `--badge-ring`。状态本身也在按钮的 `title` / `aria-label`
    （「账号，已确认/未确认 QQ」）里，所以这个点是装饰，带 `aria-hidden`。
    - **它是「加回来」的，而且比原来小**：之前那版是 12px，后来按用户要求整个拿掉了
      （只留 `title`/`aria-label`），现在按用户要求加回来并要求「小一点」—— 12px 在 40px
      上读起来像第二件元素，9px 才是「角上的一个徽标」。圆环也按比例收到 1.5px。
    - **按钮不能裁，裁的是里面那层 `.clip`**：这个点坐在方块的角上，而按钮的
      `overflow: hidden` 会按圆角的弧把它咬掉一块（6× 下看得很清楚）。所以
      `MarkNote.js` 里 canvas 被包进一个 `inset: 0` + `border-radius: inherit` 的
      `<span class="clip">`，裁切归它，按钮本身不裁。**不要再把 `overflow: hidden`
      加回 `.mark`**。音符不需要这层：它本来就在盒子内，投影由 `<svg>` 自己裁。
  - **尺寸从不变化，播放也不变**：`.mark` 就是 40px 圆角方块（`width`/`height` 都是 40、
    `flex: 0 0 auto`），从第一帧起就是这个尺寸，header 里的搜索和操作按钮保持原本的布局与
    点击区域。`.mark` 的 `transition` 只剩 `transform`（按压态）——**不要再给它加 `width`
    过渡**，那会让人以为还有什么在改它的尺寸。
    - **它曾经有一条进场：先是一条伸到搜索按钮前的宽条，再收成方块**（`.mark-intro` +
      `flex-grow: 1`，宽条的真实像素宽在 `useLayoutEffect` 里量出来用内联 `width` 钉住，
      460ms 后撤掉）。**已按用户要求整条删掉**：一上来就是正常大小，别再恢复。
  - **唯一进场的东西是音符：一秒淡入**（`.art` 和 `.pane` 共用一条 `note-in` CSS animation，
    `1s ease both`）。两层一起淡入，所以毛玻璃和音符是一起到位的；规则只写一次，
    改时间不会让这两层走散。全在 CSS 里，组件不持这个状态 —— 所以没有任何路径能把音符
    卡在半透明。`both` 是必需的：没有它，第一帧会先以全不透明闪一下，动画才接手。
    `prefers-reduced-motion` 下两层都 `animation: none`，音符就在那儿（基础规则本来就是
    `opacity: 1`）。
  - **它是应用自己的图标，但拆成了四层**：底图是 `<canvas>` 现场画的固定彩虹印谱
    （包在裁切层 `.clip` 里），上面一层是毛玻璃 `.pane`，音符是画在它上面的一条静态
    `<path>`，角上是 QQ 状态点。
    底图原本是 `public/mark-bg.jpg`，那张位图连同 `assetUrl('/mark-bg.jpg')` 规则一起
    拿掉了 —— **底图不再是一张图，也不再需要 `assetUrl`**。`MarkNote.js` 挂载时只画一次
    320×160 缓冲；音符没有 `transform`，不会呼吸、跳动或放大。
  - **音符是毛玻璃，不是白色剪影**：两层，因为这是两件事 —— `.pane` 把**音符背后**的彩虹
    糊掉，`.ink` 再用半透明白（`rgba(255, 255, 255, 0.62)`）画出音符本身。
    - **`backdrop-filter` 糊的是元素*背后*的东西，所以它不能挂在音符上**：音符要保持锐利，
      就必须有个东西坐在它下面变软。
    - **被遮罩裁住的 `backdrop-filter` 会被这个遮罩裁掉** —— 6× 下确认过（同一页并排
      「有遮罩 / 无遮罩」，无遮罩那张整个 40px 方块都糊了）。这是整个做法能成立的理由：
      没有别的办法把一个模糊限制在任意形状里。
    - **遮罩由组件生成，不写进样式表**：`NOTE_MASK` 用 `NOTE` 拼成一条 data URI，通过内联
      `style` 挂上去。样式表里再抄一份轮廓，迟早会和图标走散，而且**走散了看不出来** ——
      玻璃只是不再对齐它本该是的那块玻璃。冒烟测试把遮罩里的 `d` 和音符自己的 `d`
      逐字符比一遍（577 字符，必须相等）。
    - **`.62` 是挑出来的，不是推出来的**：拿真实的 canvas、真实的投影在 6× 下比过三个
      白度 —— 0.45 只有凑近看才认得出音符，1.0 就是被换掉的那个不透明白剪影。40px 上
      音符得有点「体量」，才还是这个标记的主角。
    - `saturate` 不是装饰：只糊不饱和，带子会被洗白，而这个彩虹本来就是饱和的。
  - **印谱是七条渐变彩虹，不是七条平色**：`BANDS` 从红排到紫，每条带记的是**两个色**——
    顶端色和底端色，因为参考图上的带子是渐变的（橙从 `#f6541c` 到 `#f9a832`，浅黄那条几乎
    不变）。相邻两条带在共享边界上取**和邻居各一半的混色**，所以跨过波浪边界时颜色是连着的，
    边界本身又仍然读得出是一条边。带子只占缓冲区中间 5/6（`VISIBLE_FRACTION`，对应
    `.bg` 的 `height: 120%` / `top: -10%`），剩下那 1/6 是波幅的余量，作用是把平的红色顶边
    和紫色底边留在可视区外。
  - **波浪的位置来自实测，不是猜的**：`BOUNDARIES`（七条带静止时的高度边界，按高度的比例）
    和 `SWING`（每条边界摆多少）都是从参考图上量出来的，两个数组都不均匀 —— 带子高
    0.11~0.18，中间那几条边界的摆幅只有外侧的一半，这个收腰正是参考图的形状；给所有边界
    一个振幅会让彩虹看起来是「鼓」起来的。`WAVE_ORIGIN` 是波峰在缓冲区里的 px 位置：
    参考图的波峰在横向五分之一处，不在边缘。**七条边界共用同一道波**（`ripple()` 只跟 `u`
    有关），各按自己的 `SWING` 摆 —— 参考图上七条带是一起飘的，不是互相错相位地编辫子。
  - **底图是逐列画出来的，不是一条画布宽的渐变**：`drawBackdrop` 每 1px 列建一条
    `createLinearGradient(0, 0, 0, DRAW_H)`，停点落在**那一列自己**的波浪位置上。一条画布宽的
    渐变弯不了，它的停点会沿固定 y 切出一条水平的混色带横在波浪上 —— 40px 下看就是带子中间
    一道淡淡的接缝。列与列刚好铺满，没有缝。列内每条带的两端各留 `BLEND`（12%）做混色，
    而混色的宽度按**那条带自己的高度**算：这些带会收到平均高度的三分之二，固定宽度会让一条
    带的混色溢到邻居身上。
  - **播放时彩虹一直向右流**：`rainbow-score-flow 2s linear infinite` 挂在 `.bg` 上，
    一个周期正好平移一个波长（自身宽度的 `50%`，即 `WAVE_LAMBDA = DRAW_W / 2`），
    首尾帧相同，所以循环无缝。`linear` 是必需的：一缓动就会在每个波长处看得出停顿。
    canvas 做成 260% 宽、-140% 靠左，多出来的 140% 就是这一整周期行程的余量 ——
    这两个数和 `WAVE_LAMBDA` / `WAVE_ORIGIN` 是**同一笔账拆在两个文件里**，改一个波峰就换位置。
    - **暂停/播放切的是 `animation-play-state`，不是「有没有动画」**：`.mark-playing .bg`
      只写 `running`，`.bg` 自己带 `paused`。**不要改回「暂停时把动画摘掉」** ——
      摘掉动画会把 canvas 弹回 `translate(0)`，而一个周期正好一个波长，所以这一跳只有在
      周期边界上才看不出来，真实暂停不会正好落在那里，看起来就是背景硬切一下。
      `paused` 则停在当时的帧，再播从那儿接着走。同一招在 `.disc-paused`（唱片）和
      `.eq-paused i`（均衡条）上已经用了。
    - 不读音频、不拆频段、不重画 canvas。因为没有 Web Audio 监听，切后台不会牵连 logo 的动画链路。
  - **它曾经是访客头像**（绑了 QQ 就是 QQ 头像）。那个盘子连同它的全部讲究搬去了
    「账号」页的身份卡（`Account.module.scss` 的 `.identity-avatar`）——**一个头像说的是
    「听歌的是谁」，而这个按钮说的是「设置在哪」**，两件事不该长在同一个位置上。
    盘子的那几条约束一条都没变，只是换了主人：
    - **默认头像读成头像，靠的是「填满 + 挖空」这个结构**：一个**填满的盘子**
      （`--avatar-bg`）+ 白色**挖空**音符（`--avatar-fg`）。浅底 + 深色字形在**任何**
      不透明度下都读成「一个白圈里有个图标」，读不成头像 —— 所以这不是配色偏好，
      是底色和字形各司其职：盘子负责「这是个人」，字形负责「现在还没有人」。
    - **底色是 Apple Music 那种红渐变，而且两端都不能靠近白**：`linear-gradient(150deg, …)`，
      浅色 `#fb5c74 → #fa233b → #d81b34`、深色 `#f4475f → #e01f3d → #b8162c`。
      浅色那端压着的 `#fa233b` 就是 `--row-active` 已经在用的红，不是新引进的颜色。
      白色的音符要压着**整条**斜坡，所以两端都得是饱和的：只要有一端变浅，音符在那一头就没了。
      纯色盘子在 40px 上像一块色卡，渐变让它读成一张图。
    - **盘子上不再有毛玻璃遮罩，而且是有意去掉的**：曾经有一层 `.avatar-glass`
      （14% 白 + `backdrop-filter: blur(2px)`），那是给**纯色**盘子准备的 —— 纯色底需要一个
      「隔着玻璃」的层次；渐变底不需要，它自己已经是亮的表面，而遮罩唯一能模糊的东西
      就是那个音符本身（也就是这个盘子存在的全部理由）。**换渐变时不要顺手把遮罩加回来**，
      `--avatar-bg` 是这件事的全部。
    - **`IconNote` 的 `filled` 是实心写法，不是描边加填充**：竖线/横梁是填充块、符头是斜椭圆，
      和 SF Symbols 的 `.fill` 一个意思。描边版 21px 时笔画只有 ~1.75px 宽，不够厚。
      列表行里的缩略图仍然用描边版。
    - **「居中」有两半，缺一半都不算居中**：flex 盒子管「盒子在盘子里居中」，
      而字形**自己在 viewBox 里**要居中 —— `IconNote filled` 整体下移 0.88（横梁到 y 1.4、
      最低的符头到 y 20.84，外框中心在 11.12 而不是 12）。居的是**外框**不是墨迹：
      重量压在两个符头上，按墨迹居中的话横梁会被顶上去，整个符号看起来是飘着的。
  - **它仍然是按钮，因为背后终于有东西了**：点它升起「账号」抽屉页（`h5/Account.js`）。
    它曾经**故意不是**按钮 —— 那时它背后什么都没有，而一个点了没反应的控件
    比一张不会动的图更糟。**这条规律没变，变的是前提**：先有页面，再让它变成控件。
  - **曲库名没有消失，只是不上屏**：`<h1>` 改成 `.sr-only`（`clip` 式视觉隐藏，`innerText` 仍可读）。
    它是页面的唯一标题，也是驱动器判断「切了曲库」的唯一观察点（`SOURCE_TITLE` 读 `h1.innerText`）——
    改成 `display: none` 会把它从 `innerText` 里也拿掉，等于悄悄删掉那个观测点。
  - **栏本身照旧 sticky**。它曾经有过一版「跟着列表滚走、滚走后浮出毛玻璃浮动条」，
    已经删掉：两条 bar 轮流上场的机器比这个 header 值得的复杂度高，而交班那一下
    恰恰是眼睛唯一会注意到的东西。**再去掉 sticky 时，别顺手把它作为「容器」的表现
    （玻璃 / 通栏 / 投影）也删掉** —— 那样看到的是「容器没了」，不是「容器滚走了」。
- **列表的 ⋮ 抽屉装着三件事**（`h5/MusicApp.js`）：`音乐库`（打开同名的抽屉页 ——
  这里有哪些歌、哪个文件夹、以及**连接自己的云盘**）、`缓存管理`、`切换外观`。
  它中间被删过一次 —— 那时缓存管理和切换外观搬去了账号页，抽屉只剩 `谷歌云盘链接` 一个条目，
  而**只有一个条目的菜单比没有菜单更糟**：多一次点击、多一次判断，换来的东西本来可以是一行。
  现在它装的是**关于这台设备、这个曲库、以及它被画成什么样**的三件事，和「账号」抽屉页
  （关于这个人）分工一致 —— **一个菜单值不值得存在，看它是不是不止一件事**。
  - **`谷歌云盘链接` 并进了 `音乐库`**，抽屉不再有它。抽屉曾经同时回答同一个问题两次：
    「我现在在哪个曲库」是「音乐库」面板里的卡片，「连接自己的云盘」是抽屉里的另一个条目 ——
    一件事两次点击。现在抽屉只有一个曲库入口，而连接动作**就画在答案旁边**（卡片下面那一行，
    见「音乐库」一节）。
  - **`切换外观` 从账号页搬回这里**：它是**这个屏幕**的属性，不是「听歌的是谁」的属性，
    而抽屉就是关于这台设备的菜单。那一行戴的是**要切过去的那个主题**的图标（和它在账号页时一样）。
  - 抽屉、`menuOpen` / `menuClosing` / `closeMenu` / `openMenu` / Escape 监听都挂在
    `MusicApp` 这一层：列表是这些面板**升起在其上**的那块屏幕，而 `position: fixed`
    嵌在滚动列里会被算成那个列的尺寸，不是视口的尺寸（列上曾经还有 tab 过渡的 `transform`，
    抽屉会停在整页滚动范围的最底部 —— 那更糟）。
  - **⋮ 本身不跟 `connected` 走**：曲库空的时候正是最需要 `音乐库` 的时候 ——
    连接云盘就在那里面 —— 而缓存管理根本不关心列表里有没有歌。
  - **行抽屉（每首歌的 ⋮）不是一回事**：它是逐首的、挂在同一层，理由相同。
  - 三个条目和四个抽屉页共用同一个「面板开合」形状（`MusicApp` 里的 `useSheet`）：
    打开即挂载、关闭先播退场动画、`prefers-reduced-motion` 下立刻卸载。
    `usePlayer` 里那三份（播放页 / 缓存 / 云盘）没动 —— 它们的 state 属于数据的所有者。
- **「账号」抽屉页（`h5/Account.js`）是访客自己的面板**，从列表**最左边**的应用图标升起，
  和「音乐库」（`h5/Profile.js`）分工明确：**「音乐库」管「这里有哪些歌」，「账号」管
  「听歌的是谁」**。它装了 QQ 号确认和「我的数据」（`听歌排行` 入口 + `数据同步`）；
  `缓存管理`、`音乐库`、`切换外观` 都在列表的 ⋮ 抽屉里 —— 那是关于设备、曲库和这个屏幕的菜单，
  而这个面板是关于人的。
  - **它是一个「抽屉页」，不是有 header 的页面**（`SheetChrome`，和 `缓存管理` / `谷歌云盘链接`
    同一套外壳：从底部升起、顶部只有「收起」按钮 + 标题）。所以它没有页面标题栏，也没有
    那一排 `nav-btn` 胶囊 —— **一个抽屉里再放一行目的地，就是一个假装自己是面板的页面**。
    到别的屏幕的路在 ⋮ 抽屉里，回列表的路就是那个「收起」。
  - **号码是一张卡、两种状态：有号就只画卡，没号就只画确认块**（二者互斥，永远不同屏）。
    它曾经是上下两块 —— 上面一张显示**已存**号码的身份卡，下面一个装着**刚敲进去**内容的表单 ——
    于是「我的号到底存上了没有」在屏幕上同时有两个半答案：访客把卡片、输入框的 placeholder
    和头像三个信号都看完，仍然不能确定。现在：有号码 → 卡片（名字 + `已确认` 徽标 + 头像）
    **加卡片自己的「清除」按钮**；没号码 → 确认块（输入框 + `确认` + 提示）。
    清除和 `取消置顶` 是同一类动作（访客自己敲进去的，就得能撤回），而且**清完就回到他敲进去时
    那个块** —— 那个「换一个 QQ 号」的 placeholder 也因此没了，表单出现时永远是空的第一格。
  - **卡片上的副标题承担了原表单那行提示的活**：表单不在屏上时，「听歌次数和喜欢都记在这个
    号码下」得有地方说，所以它和头像来源写在同一行（头像取不到时那句话也要承认，否则默认音符
    看起来像应用无视了刚输入的内容）。
  - **`SheetChrome` 的 `.page` 带 `role="dialog"` / `aria-modal` / `aria-label={title}`**：
    面板本来就该被读屏念成对话框，而标题正好是它的名字；顺带它也是冒烟测试打开这些面板的
    抓手（`[role="dialog"][aria-label="账号"]`），于是选择器不必知道任何哈希类名。
  - **页面用两种方式说「号码到底确认上没有」**：身份卡上的 `已确认` / `未确认` 徽标（一句话），
    和列表应用图标右下角同色的小点（一眼）。在这之前，屏幕上**没有任何东西**回答这个问题：
    输入框里留着刚才敲的字、标题两种状态都显示一个号码、头像可能只是没加载出来 ——
    三个信号，没有一个是「确认」。
  - **文案统一用「确认」，不用「绑定」**，因为那才是访客做的动作（按钮也从 `确定` 改成 `确认`）。
    没号码时卡片整个不在屏上，所以不再有 `访客` 这个名字、也不再有 `未确认` 这个徽标 ——
    **「还没有号码」现在由「屏上是一个空的确认块」表达**，比一个写着「未确认」的徽标更直接。
    （列表图标右下角那个灰点仍然是「未确认」的第二种说法，那是给**不去打开面板**的人看的。）
  - **徽标是绿的，和列表图标上那个点是同一个绿**：号码是**一个**状态，所以它在哪儿画
    都是同一个颜色。（`Profile.module.scss` 里有一对同样的徽标，是蓝色的 —— 那是
    云盘连接，另一个问题，所以有意用另一个颜色。）绿有两种写法：点用鲜艳的 `#34c759`
    （**填充**压在图上要够跳），徽标文字用 `--ok`（**文字**要在浅色徽标上读得清，
    浅色模式是 `#1f8a3b`）。同色相，两个用途。
  - **QQ 号做三件事：换头像、当播放次数的归属、当「我喜欢」的归属**（`shared.js` 的
    `QQ_KEY` / `normalizeQq` / `qqAvatarUrl`）。合法就是 5–11 位数字，**只看长度**：
    没有校验位、没有前缀规则，也没法从这里验证账号是否存在 —— 再严一点就是编造这个应用
    没有的权威，它是访客给自己头像起的名字，不是凭据。这条在有了 D1 之后**更重要了**：
    `GET /stats` 和 `GET /likes` 因此都没有鉴权，知道号码的人就能看它的排行和喜欢
    （见「听歌排行」一节）。输入先剥掉非数字（有人会粘「QQ：1234567」），
    因为拒绝这种输入教不了任何人任何事。
  - **号码本身归 `usePlayer`，不是归这个面板**（`usePlayer` 的 `qq` / `saveQq`）。它以前是
    `h5/MusicApp` 的 state，那时它只服务于头像；现在它同时是播放上报和喜欢的 key，
    而上报发生在 `usePlayer` 里（两套布局共用），一个值两处读就是「谁在听」有两个答案。
    读取放在 effect 里而不是 `useState` 初值里（静态导出，SSR 会先跑一遍），`''` 是一个真值
    （清除写的），所有读者都把它读成「没绑定」而不是「还没读到」。
  - **确认号码会把本机的喜欢一起带过去**（`adoptGuestLikes`）：访客在没号码时喜欢的歌，
    确认之后进这个号的 outbox，随队上传。所以 `useDataSync` 多了一个 `revision`（外壳传 `qq`）：
    这个动作会在访客没按任何按钮的情况下让队列变长，不重数的话，那一行会对着刚变长的队列
    继续说「本地记录已全部上传」。（面板开着时后台还在记播放，是另一种过期，**有意不处理** ——
    音乐在面板下面继续放，数字在下次按「同步」时刷新，为一件后台杂事挂一个定时器不值。）
  - **头像是 `q1.qlogo.cn` 的公开接口**（`b=qq&nk=<号>&s=100`），当 `<img src>` 用，
    所以不需要 key、token 或 CORS。**它答错号的方式是给一张占位图，不是 404** ——
    所以页面里的「取不到就退回音符」兜的是网络，不是号码写错，别把它说成校验。
  - **URL 和「加载失败」这个标志都归外壳**（`MusicApp`）。列表顶栏不再画头像之后，
    身份卡是唯一调用方，但那两样**仍然留在外壳**：它们依赖的是号码，而号码在外壳这一层
    从 `usePlayer` 拿出来 —— 一个兜底由调用方自己决定，就是第二个兜底。
  - **「清除」必须有**：号码是访客自己敲进去的，就得能撤回。和 `取消置顶` 同一类 ——
    单向操作（只能设、不能撤）是这个项目已经踩过一次的坑。**清除号码不会清掉已经排队的
    播放和喜欢**：那些记录自己带着号码（`playStats.js` / `likes.js` 的日志和 outbox 都是
    按 QQ 分组的），所以「我的数据」卡片在没绑号时仍然可能出现 —— 只要还有东西没上传。
  - **设置页的词汇表是 `settingsPage.module.scss`**（primary-btn / spinning / group /
    group-label / row / row-btn / row-icon / row-label / row-value / row-chev / hint /
    hint-bad / footnote）。这些形状曾经**故意**在 Profile / Account 里各抄一份，两边的注释
    都写着「出现第三个设置风格屏幕时就是抽取的时机」——**「听歌排行」就是那一刻**，于是把
    「我的」一起折了进来，而不是留一个屏幕用旧副本。
    后来三个屏幕全都变成了抽屉页：**它们只 `composes:` 卡片/行/提示/脚注这套词汇，外框由
    `SheetChrome` 给**，而 `.page` / `.head` / `.title` / `.nav-btn` / `.refresh-btn` 这套
    **页面外壳已经跟着最后一个页面一起删掉了** —— 手机端现在只有「列表」一个页面。
    **词汇表里只放词汇**：身份卡和 QQ 表单在 `Account.module.scss`，文件夹选择在 `Profile`，
    排行列表在 `StatsPage` —— 一个「什么都有点」的共享文件就不再是词汇表，而是没人敢改的地方。
  - **手机端只剩一个页面，`tab` 这个 state 没有了**（`MusicApp`）。列表是唯一「你待着」的地方；
    账号 / 音乐库 / 听歌排行 / 缓存 / 云盘都是升起在它上面的面板。`.view` / `.view-in` /
    `.view-off` 那套「两个 tab 都常驻挂载、保住滚动位置」的机器也一起删了 ——
    没有第二个 tab 可切，而它带来的 `transform` 祖先（`position: fixed` 会被收进去）
    正是过去所有面板都必须挂在外壳这一层的原因。
    `useSheet()` 是外壳里那个共用的开合状态；`useDataSync({ active: account.open })`、
    `usePlayStats({ qq, active: stats.open })` 跟着各自的面板走。迷你条只在列表上出现 ——
    面板是设置，不是播放界面。
  - **「数据同步」是两支队列入队，一颗按钮**（`h5/useDataSync.js`）：播放日志和喜欢的
    outbox 一起数、依次 flush。访客没有「两种没上传的数据」，只有**没上传的数据**，
    所以只报一个 `N 条`。两个 flush 都**不抛异常**（把结果当数据返回），所以这个 hook
    里的 `try/catch` 是给「意料之外」留的。文案跟着结果走而不是跟着成功标志走 ——
    部分失败才是那个有意思的情况（「已同步 3 条，还有 2 条没成功」），也正是访客能行动的。
  - **卡片什么时候出现**：`showData = Boolean(qq) || (ready && (pending > 0 || notice))`。
    `ready` 隔开「还没数过」和「数过了是 0」—— 这个 hook 在没人看的时候根本不该读 localStorage，
    更不能在没数过时声称「已全部上传」。没绑号时**不画「听歌排行」那一行**（排行是按 QQ 分的，
    没号就没有东西可开），但「数据同步」那一行可能在 —— 见上面「清除号码」那条。
- **「音乐库」抽屉页（`h5/Profile.js`）是「这里有哪些歌」那一半**，和「账号」的分工没变：
  **「音乐库」管「这里有哪些歌」，「账号」管「听歌的是谁」**。它从列表的 ⋮ 抽屉升起，
  是那三个条目里的第一个。
  - **它也是一个抽屉页**（`SheetChrome`），标题是 `音乐库`（不是 `我的`）：
    这个面板说的是曲库，而 `我的` 是一个页面名字，不是一件事。头部没有 nav 胶囊，
    右上角那个动作按钮是「刷新列表」（禁用状态即加载中）。
  - **`谷歌云盘链接` 现在是这个面板里的一行，抽屉里没有它了**。它来回搬过两次：
    从抽屉搬进来（理由是「连接是曲库的事」），又搬回抽屉（理由是「抽屉才是那个菜单」），
    现在**并了进来** —— 抽屉曾经把同一个问题问了两遍：「我现在在哪个曲库」是这里的卡片，
    「连接自己的云盘」是抽屉里的另一个条目，一件事两次点击。所以抽屉只剩一个曲库入口，
    而连接动作**就画在答案旁边**：卡片下面那一行（没连时是 `连接 Google 云盘`，
    连上后是 `谷歌云盘链接`），点开的是云盘 sheet。
  - **云盘 sheet 会盖在这个面板上**（它在 `MusicApp` 里渲染得更靠后，而所有 `SheetChrome`
    的 `z-index` 一样，所以后来者在上）。这是有意的：从「音乐库」进来连接，关掉云盘 sheet
    应该回到「音乐库」，而不是把人丢回列表。
  - **顶部那张卡是照着云盘 sheet 的卡做的**：同一个蓝色圆角图标、同一个 `已连接` /
    `未连接` 徽标。**两张卡回答的是同一个问题（我现在在哪个曲库），就该用同一种方式回答。**
    徽标是加上去的：在那之前，唯一能分辨「现在是公共曲库还是我的云盘」的办法是
    读标题、并且知道默认那个曲库叫什么名字。
  - **卡片不包在 `.group` 里**：它是这个面板自己的主体，而不是设置卡片里的一行，
    卡片套卡片读起来像画错了。所以 `.account` 自己带 `margin-bottom` / 圆角 / `--field` 底。
  - 卡片上的徽标是**蓝色**，和「账号」页 QQ 徽标的**绿色**是两对：两个不同的问题，
    有意用两种颜色（见上面那条）。
  - **没连云盘时，这个面板现在有了真正的下一步**：一张卡（哪个曲库、多少首、无需授权）、
    一行 `连接 Google 云盘`、一段说明。以前这里是一段文字，指着 ⋮ 抽屉里的一个条目说
    「去那里选」—— **一段指路的话，等于承认那个入口不好找**；现在那一步就在这里。
    （列表空的时候，那个提示按钮仍然**直接开云盘 sheet**：一个空的曲库只有一步有用的
    下一步，不该让访客先在一串选项里猜。）
  - **它曾经是一个 tab 页，而且是全应用只有一个入口的那种** —— 入口是歌曲列表的
    「曲库里还没有歌曲」提示，也就是说曲库正常有歌的时候它根本打不开。
    这个 bug 是通过给它加一对互相指向的 `nav-btn` 修的，现在改成抽屉页之后，
    入口是 ⋮ 抽屉里的一行，**同一个问题不会再回来**。

### 听歌排行（播放次数）

**播放次数和「我喜欢」是这个应用里唯一会离开浏览器的数据。** 别的都在本机（音频缓存、置顶、
QQ 号），这两个不行 —— 它们天生是「跨设备看同一个人」的功能，所以要有个不在浏览器里的地方：
Cloudflare D1，由 `cloudflare-worker/` 的 `POST /plays` / `GET /stats`（播放）和
`GET /likes` / `POST /likes`（喜欢）读写。**两个都用 QQ 号当归属，都挂同一个 `PLAY_DB` 绑定。**

三个文件，各管一段：

| 文件 | 管什么 |
| --- | --- |
| `components/Music/playStats.js` | 本机待同步日志、非阻塞上报、拉统计、`formatAgo` |
| `components/Music/h5/usePlayStats.js` | 「听歌排行」面板的状态（加载 / 错误 / 刷新） |
| `components/Music/h5/useDataSync.js` | 「账号」页「数据同步」那一行：两支队列的计数 + 手动同步 |
| `cloudflare-worker/src/index.js` + `schema.sql` | 落库、去重、两个窗口的排行查询 |

**五条不变量，改任何一处之前先想清楚会不会破掉它们：**

1. **不挡听歌。** 上报挂在 `<audio>` 的 `play` 事件上，`recordPlay` 只做两件事：写几百字节
   到 localStorage，然后**发一个没人 await 的请求**。任何一步都不能变成 await 链上的一环 ——
   访客等的是音乐，不是数据库。
2. **失败不丢。** 事件先写本机日志，服务端确认后才删。断网 / Worker 挂了退化成「以后再传」，
   而不是「这次不算」。同步是幂等的（见下一条），所以自动重试和手动「同步」按钮可以并存。
3. **重发不重复计数。** 每个事件带一个客户端生成的 `eid`，它是 D1 的主键，服务端
   `INSERT OR IGNORE`。没有这一条，重试就会虚增次数 —— 而次数是这个功能的全部内容。
   本地日志**只删服务端确认过的那些**（`flushPending` 拿确认集合去 filter，不是整份清空）。
4. **两边的批次上限必须一样**（`playStats.js` 的 `BATCH_SIZE` 对 Worker 的
   `MAX_PLAYS_PER_REQUEST`，都是 200；`likes.js` 的 `BATCH_SIZE` 对 `MAX_LIKES_PER_REQUEST`，
   都是 500）。服务端对超出的部分**照样回 200**，客户端于是把整批标成「已确认」再删掉 ——
   多出来的那几条就静默丢了。日志上限是 1000 条，所以只有很久没联网的设备会撞上，
   但那正是这个日志存在的场景。**两对常量是分开的**，改一对不要以为另一对跟着变。
5. **Worker 里建表用 `batch`，不要用 `exec`**：D1 绑定的 `exec()` **不吃多语句字符串**，
   整串丢给 SQLite 会回 `D1_EXEC_ERROR: incomplete input` —— 第一次部署就是这么挂的。
   `schema.sql` 和 `index.js` 里的 `SCHEMA_STATEMENTS` 是同一份 DDL 的两个写法，改一处
   记得改另一处（`wrangler d1 execute --file` 那条路能跑多语句，它自己会拆）。
   建表是**懒执行**的（第一次请求时跑一次，`schemaPromise` 缓存住），所以加一张新表
   不需要额外的部署步骤 —— 但**第一次调用会慢**，而且失败要能重来（`catch` 里把
   `schemaPromise` 清回 null）。

**一次播放要「真的听了 10 秒」才算**（`PLAY_COUNT_AFTER_SECONDS`）：计数在
`onAudioTimeUpdate` 里按 `<audio>` 自己的 `currentTime` 累加，只累加**没暂停时**的增量，
所以「点一下播放、两秒后切走」、被浏览器挡掉的自动播放、把进度条拖到底，都不算一次 ——
这些都不是在听。阈值那个数字放在 `playStats.js`（计数规则都在那儿），但**执行只能在
播放器里**：`recordPlay` 是个不带时间的纯写入，只有 `<audio>` 知道有没有真的在出声。
累加时要把 seek 排除掉：`timeupdate` 一秒约四次，正常增量是零点几秒，往前跳是拖进度条、
负数是往回拖，两者都不算听；上限 2.5s 松到不会被卡住的主线程误伤。
同一个 `track.id` 记满一次就不再记，否则暂停/继续会把它变成「点了多少次屏幕」；
**单曲循环要在 `handleEnded` 里把计数清掉**，不然循环一整晚只算一次。
没有绑定 QQ 就整条不记 —— 排行是按 QQ 分的，记一条归不到任何人名下的播放没有意义。

**「听歌排行」（`h5/StatsPage.js`）现在是「账号」面板升起的一个抽屉页。** 它搬过两次家：
先是账号页底部一张卡片，然后是一个**独立的 tab 页**。第二次搬家的两个理由仍然成立 ——
五十首歌的列表是一个**目的地**而不是一张设置卡；而且它只对绑了号的访客有意义，
不是一张必须解释自己为什么是空的卡片。

但**「独立成页」这个做法本身错了，现在改成了面板**。作为页面，它没有 tab 栏可待，
于是它自己的 header 里放了一颗「账号」胶囊，而「账号」里唯一通向它的又是一行「听歌排行」——
**两块屏幕互相指着对方，中间没有第三个出口**。访客进到排行页之后，唯一的动作就是回账号页，
回到账号页之后唯一的动作就是再进排行页。现在它是 `SheetChrome`（和账号 / 音乐库 / 缓存 /
云盘同一套外壳）：由账号面板的那一行升起，**账号同时收起**（否则它的「收起」按钮会掀出
另一个面板 —— 那不叫收起），而它自己的「收起」**直接回到歌曲列表**。一个抽屉是「离开」，
不是「导航」。

`usePlayStats({ qq, active: stats.open })` 里的 `active` 是「这个面板正开着」，
不是「挂载了」—— 否则每次打开应用都要拉一次排行。因为面板是按需挂载的，
`scope`（全部 / 最近 7 天）每次打开都重置，这是对的：它不值得被记住，
而一个回来就停在「最近 7 天」又说不清为什么的面板读起来像 bug。

**两份数据来自两个查询**（全部 / 最近 7 天是同一个 SQL 换 `WHERE`），
所以列表上那行汇总必须跟着当前 scope 走，不能用同一个 total 写两遍。
列表为空时**不画汇总**（「共播放 0 次」和「还没有播放记录」是同一句话说两遍）。
没绑号时面板画的是**一句解释 + 一个「去确认 QQ 号」按钮**（`primary-btn`），不是空列表 ——
这条路实际上只可能在「面板开着的时候号码在另一个标签页里被清掉」时走到，
但状态是可能的，所以它得有话可说，而不是画一个空排行。

**「同步」按钮现在在「账号」抽屉页，而且管两支队列**（`useDataSync`，见「账号」那一条）。
它从一个按钮两种长相（有本地记录时是实心、没有时是描边）没变，和 `置顶` / `喜欢` 同形：
队列空着的时候还杵一个大红按钮，是在邀请一次什么也不做的点击。**它曾经在排行卡片上**，
搬走的原因是：队列是访客**号码**的事，不是他排行的事；而且喜欢也需要同一个按钮 ——
两份「什么是还没上传的」迟早会给两个答案。

**没有鉴权，是有意的。** QQ 号不是凭据（见「账号」页那几条），所以 `GET /stats` 回答的是
「这个号码听过什么」、`GET /likes` 回答的是「这个号码喜欢什么」。要保护它们得先有登录，
不是在接口上加个参数。这条写在 README 里给访客看，也写在 `cloudflare-worker/README.md`
里给改的人看。

### 我喜欢（D1）

喜欢原本是一份本机数组（`LIKED_KEY`），和置顶同形。现在它是**按 QQ 号存在 D1 里的**，
由 `components/Music/likes.js` 读写。搬家的理由和播放次数一样：它属于「这个人」，
不属于「这台设备」。

**缓存策略是这一节的全部内容**（`likes.js` 顶部有同样四条的注释）：

1. **服务端为准，镜像是缓存。** 每次成功的 `GET /likes` **整份替换**镜像。没有 TTL，
   也不需要 —— 喜欢不会过期（音频缓存的 30 天是反例），「用真相替换」比「过一阵子失效」
   既简单又正确。
2. **拉取失败绝不清镜像。** 和列表缓存（`readListCache`）同一条规矩：断网意味着
   「继续显示我们原来有的」，不是「你没有喜欢」。
3. **镜像是按访客分键的**（`music:likes:<qq>`，没号码时是 `music:likes:guest`）。
   换号绝不能显示上一个号的喜欢 —— 和列表缓存按来源分键同一个理由。
4. **写是乐观的、进队列的。** 一次点击立刻改镜像（心马上亮），并把**期望状态**记进 outbox；
   请求 fire-and-forget，没被确认的一直留在队列里，下次再发。只有被确认的才从 outbox 删。
5. **界面看到的是「镜像 ⊕ outbox」**（`readLikedKeys`）。outbox 里还没被确认的条目**压过**
   镜像 —— 否则一次恰好在点击和确认之间落地的 `GET /likes` 会把屏幕上那一下撤销，
   等队列 flush 完再翻回来。没被确认的决定仍然是访客的决定。

**为什么必须有镜像**：列表是**每行每次渲染**都问一次「这首喜欢了吗」，而它同时要能离线
工作（音频缓存是唯一的离线能力，那是有意的）。网络往返不能在这条路径上，await 更不能。
所以喜欢集合以 `[{ id, name, at }]` 的 JSON 存在 localStorage 里，**同步读**，
第一次绘制就是对的。`readLikedKeys(qq)` 因此是同步的。

**没确认 QQ 也能喜欢，只是只在这台设备上**（`guest` 桶）。`bucketOf(qq)` 把空号码映射成
`'guest'`：它和号码桶一样是一个镜像，但 `applyLikeChange` 对它**直接 return** ——
不写 outbox、不发请求，所以「只在本机」不是一条需要维护的策略，而是那个桶里根本没有
出网的东西。`refreshLikes('')` 也不发请求，直接返回镜像：**没号码就没有行可读**，
去问服务端等于在问别人的喜欢。`readOutbox` 会把 `guest` 键丢掉，所以就算旧版本写进去过
一个队列，它也不会被 flush。

**确认号码时，本机的喜欢跟着号码走**（`adoptGuestLikes`）：那些歌是访客在这台设备上
真点过的，确认一个号不该让它们消失或者被留在原地。它们进这个号的 outbox（随队上传，
离线也成立），然后 **guest 桶被清空** —— 这一步是必须的：留着它，每次再确认一次号码都会
重新收养同一批歌，包括访客之后在数据库里**取消**过的那些，等于让它们复活。

**outbox 是「track id → 期望状态」的映射，不是点击日志。** 这是它能折叠整个离线会话的原因：
同一首歌喜欢又取消三次，最后只是一个 `liked: true` 或 `false` 的条目，服务端的
`INSERT OR IGNORE` / `DELETE` 收到多少次都收敛到同一个状态。（隔壁的播放日志**是**日志，
因为一次播放是一件发生过的事；这个是一个状态，状态会互相覆盖。）
`flushLikes` 只删**期望状态仍然等于发出去那一个**的条目 —— 请求在飞的时候访客又点了一下，
那个更新的决定必须活下来（所以它是发完之后**重读** outbox 再删，不是用发之前那份）。

**那条「先绑定 QQ 号才能喜欢」的拒绝已经删掉了。** `toggleLike` 现在只剩一个拒绝
（Drive 曲目），而 toast 会说明落点：没号码时是 `已喜欢：<歌名>（只存在本机）`。
行抽屉里那一项的副标题同理，跟着 `qq` 走（`按你的 QQ 号保存` / `只保存在本机`）——
**访客唯一看不见的就是「这次点击到底存到哪去了」，所以每一处都要说。**
旧的那份本机 `liked`（`LIKED_KEY`）由 `migrateLegacyLikes(qq)` 在绑号时塞进 outbox
（**再删掉旧 key**，第二次跑不能重复入队），所以升级不会看起来像「更新吃掉了我的喜欢」；
它走的是同一条 outbox，离线也能用。没号码时它落进 guest 桶 —— 那份本机数组本来就是
「这台设备的」，而那正是 guest 的意思。旧的键是 `<source>:<id>` 形式，只有 `cloud:` 的
被留下 —— 和 `toggleLike` 拒绝 Drive 同一个理由。


## 安全姿态

**威胁模型一句话**：静态站没有服务端，所以攻击面**全在 `cloudflare-worker/`**；
那里的风险是「被刷」和「被读」，不是「被拿下」。前端那部分要守的只有一条（下面第 2 条），
但它是最贵的一条。**别把这一节当成待办清单 —— 它记的是「什么是有意的、什么是不能碰的」。**

### 1. 已经干净、不用再改的部分

- 站点是 `output: 'export'` 的纯静态产物，**没有服务端、没有凭据**。有一个 Service Worker
  （`public/sw.js`），但它只缓存本站同源的 GET 响应，不读也不存任何凭据，攻击面没有变化。
- D1 的每一条 SQL 都走 `?1` 绑定参数。唯一拼进语句里的是模块常量
  （`RANKING_LIMIT`、`MAX_LIKES_PER_REQUEST`）→ 没有注入面。
- 仓库**和全部提交历史**里 `client_secret` / `AIza` / `-----BEGIN` / `ghp_` 零命中。
- R2 桶根路径 404，不能列目录。

### 2. 必须守住的形状：存储型 XSS

这条不是「已经修好了」，是**「现在正好是安全的」**：

> 开放写入口（`POST /plays`、`POST /likes` 谁都能写）+ `track_name` 是访客自己传的字符串
> + 它会从 `/stats`、`/likes` 原样读回来 + 它会渲染进「听歌排行」和「我喜欢」。

也就是说，**一条完整的存储型 XSS 通道已经搭好了，唯一挡住它的是 React 的自动转义。**
所以：

- **任何时候都不要**把服务端回来的字符串交给 `dangerouslySetInnerHTML` / `innerHTML` /
  `eval` / `new Function`。歌名、歌词、`track_id`、QQ 号全算「服务端回来的字符串」。
  想给歌名加粗、上色、加徽章，用 JSX 拆成元素，不要拼 HTML 字符串。
- 服务端侧已经加了 `X-Content-Type-Options: nosniff`（在 `withHeaders` 里统一加，
  缓存 HIT 分支单独补一次），**但那是第二道**。第一道是上面那条纪律。
- 长度在两端都截断了（`MAX_TRACK_NAME_LENGTH` = 200），**这不是 XSS 防护**，
  只是防止有人往库里塞一篇小说。

### 3. 无鉴权是设计，不是遗漏

`normalizeQq` 检查的是**形状**（5–11 位数字），QQ 号是访客自己填的**标签**，
不是凭据 —— 没有东西可以用来验证「你真的是这个号」。所以下面这些是**已知且接受**的后果：

| 谁能做 | 做什么 |
| --- | --- |
| 知道某个 QQ 号的人 | `GET /stats?qq=` 读它的播放排行、`GET /likes?qq=` 读它的喜欢 |
| 知道某个 QQ 号的人 | **`POST /likes` 带 `remove` 能删掉它的喜欢 —— 全站唯一的破坏性操作** |
| 任何人 | 往 `plays` 刷任意条数的假播放（`MAX_PLAYS_PER_REQUEST` = 200／次） |

改这块之前先想清楚：真加鉴权要引入账号体系，而现在的产品前提是「访客填个号码就能用」。
**要收窄的话，收窄「删别人的喜欢」那一处（比如给写入加一个服务端签发的 per-visitor 令牌），
不要动读。**

### 4. `ALLOWED_ORIGINS` 不是访问控制

`corsHeaders()` 只决定**浏览器**允不允许读响应。`curl -H 'Origin: https://evil.example'`
照样 200 —— 这个已经实测过。它的作用是让线上域名能用，**不是让陌生人不能用**。
别把它当成防线写进注释。

### 5. 限流不在代码里

Worker 里没有任何速率限制，免费版 D1 的日写入量是**十万行量级** ——
`MAX_PLAYS_PER_REQUEST = 200` 意味着**几百个请求就能打满一天的配额**，
之后的真实访客数据会**静默记不上**（客户端只看到写入失败，用户看不到任何异常）。

必须做的运维动作在 Cloudflare 面板（**代码改不了**，写在 `cloudflare-worker/README.md`
的「安全姿态」一节里）：Rate limiting rules（60 次／1 分钟／IP，匹配 `/plays` `/likes`）
+ Bot Fight Mode。

代码里唯一的天花板是 `?refresh=1` 的 **15 秒冷却**（`withinRefreshCooldown`）——
那是最便宜的「让 Worker 干最贵的事」的入口，而且它只用 `caches.default` 存时间戳，
**任何失败都 fail-open**（宁可多列一次桶，也不要让「重新载入」变成一个悄悄失效的按钮）。

### 6. 错误信息不回显

`backendError()` 把真实错误（D1 / R2 的报错里带表名、绑定名、SQL 片段）打进
`console.error`（`wrangler tail` 能看），对外只回一句 `${what}. Try again in a moment.`。
**新加 try/catch 时别顺手把 `error.message` 塞进响应体。**

### 7. `/music/` 和 `/space/` 同源，共享 localStorage

两个站点都在 `ianyspace.github.io` 下 → **同一个 origin → 共享 localStorage**，
包括 `music:googleToken`（`TOKEN_KEY`，见 `components/Music/shared.js`）。
这意味着：**别在 `/music/` 里加任何能被注入的东西** —— 一次 XSS 拿到的不是一个播放器的
播放列表，是隔壁博客的 Google 授权。这条是第 2 条的实际代价。

## 检查脚本已删除

这里原来有 11 个 `scripts/check-*.js`，在 CI 里兜 `npm run build` 抓不到的问题
（纯 CSS 的定位数字、跨文件的名字握手、只能靠时序才暴露的行为）。**已全部删除**，
连同 `.github/workflows/deploy.yml` 里对它们的调用。`Verify build output` 那一步
剩下的只有对 `out/` 里文件是否存在的断言，那些还在。

**这意味着什么**：下面这些约定从「有机器守卫」变成了「靠人读文档 + 人眼比对」。
改到相关代码时请格外小心，它们的共同点是**坏了不报错**：

- `styles.x` 写错 → 类名被静默丢掉，元素照常渲染却毫无样式。
- 缓存读路径写了东西 → IndexedDB 事务串行化，表现为「点了播放没反应」。
- 空列表文案少传一个 `likedOnly` → 把「还没喜欢过」说成「曲库空」。
- 抽屉挂在列表列里面而不是 shell 根部 → `position: fixed` 被祖先的 `transform` 收进那一列，
  跟着列表一起滚走，样式还在、位置全错。
- 两套布局只改一套 → 另一套只是「没有这个功能」，不报错。
- 封面叠加层的定位/绘制顺序 → 照片跑到别处、或盖住播放按钮。
- 播放顺序的「mount 恢复 + 变化持久化」拆成两个 effect → 把访客的选择覆盖掉。

`scripts/preview-*.js` **保留**，它们是纯视觉核验（读构建产物里的真实 CSS +
硬编码 markup 生成单文件 HTML），不属于 CI，也不随这次改动消失。
**但它们自己也会烂，而且烂了不报错 —— 只有你运行它才知道。** 每个 `idOf()` 都是硬查表，
类名没了就抛异常；两个脚本（`preview-covers.js`、`preview-desktop-list.js`）查的还是早就
不存在的旧桌面类名（`panel-open` / `panel-tools` / `brand-*` / `rail-btn`…），
现在跑不起来，修它们等于照当前 markup 重写。跑得起来的四个是
`preview-empty-list.js`、`preview-locate-btn.js`、`preview-player-drawer.js`、
`preview-mark.js` —— 改到相关 UI 时顺手跑一下，尤其是 `emptyListMessage` 那种「只改了文案」的改动。

`preview-mark.js` 值得单独说一句，因为它验证的是**截图看不见的东西**：logo 那枚 canvas
底图，播放时的 CSS 循环要无缝，前提是**底图本身在一个波长上严格周期**。这个脚本除了画出来，
还会读 `getImageData` 自己量两件事 —— 每个像素列与「向右一个波长」的像素列是否完全相同
（差一个数就是接缝），以及波浪边界那条混色带实际有多宽。后者用「像素是否落在该条带两个
端点色的**连线上**」来判定：带子内部精确落在这条线上（渐变就是这两色之间的直线插值），
所以落在线外的行正好是它和邻居交接的那段肩部 —— 拿「未压缩的斜坡」当基准会把大半条带
都判成混色。报出来的值比设计值 `2 × BLEND` 略小，因为肩部最外面那一丝按定义就在容差内。
这两个数在 40px 上都看不出来。
`#zoom` 哈希会把一枚播放中的 logo 放大 5 倍单独显示，方便肉眼看波形。

它现在也承担**音符的材质**：那层毛玻璃（`backdrop-filter` + 半透明白）在 40px 上很容易
被看成「音符只是变淡了」，只有把真实的样式表、真实的 canvas、真实的投影放在一起放大才判断
得了 —— `.62` 那个白度就是这么挑出来的（页面里也留了一行读数，免得下次又要翻样式表）。

（原来还有第四个 `preview-avatar.js`，给顶部栏那个头像比较毛玻璃半径用的。头像上那层遮罩
已按用户要求去掉，**量具跟着一起删了 —— 它存在的唯一理由就是那层遮罩**；留着它的话，
文件头那句「a music note behind a low-blur glass cover」会变成谎话。以后若再要对
`backdrop-filter` 做取舍，记住两点：模糊半径只有像素能回答（断言看不见这种东西），
以及截图要放大**结果**（`--force-device-scale-factor=4`）、**不要放大被测元素本身** ——
半径是 CSS 像素、字形 21px，把盒子 `width/height` 调大等于改掉那个正是问题的比值。）

### 写断言时仍然成立的几条约定

万一以后重新加回校验脚本，这几条坑别再踩一遍：

- **每个断言都要做变异验证**：把源码改成错的，确认脚本真的红。抓不到的断言等于没写，
  而且比没写更糟 —— 它会让人以为这块有保护。变异脚本不必提交，跑完删掉。
- **别用 `^\.foo \{[\s\S]{0,N}声明` 这种窗口去断言样式**，用 `blockOf(scss, selector)`
  抠出整条规则再断言。窗口匹配不到时，**否定断言会免费通过**（`!test()` 恒真），
  而漏写 `/m` 会让肯定断言以一个和样式无关的理由失败。两个都真踩过。
- **锚定源码时用 `\r?\n`，不要用裸 `\n`**：工作区是 CRLF、CI 是 LF，裸 `\n` 会在其中
  一边静默匹配不到。
- **`--all` 不加 `--base` 打的是线上站**（默认 `https://ianyspace.github.io/music`），
  所以「本地跑一遍 `--all`」这句话里如果漏了 `--base`，你测的是**上一次部署**，
  而失败信息看起来和本地代码一模一样。这个坑踩过一次：新写的断言在 `--all` 里红了、
  单页跑绿的，于是花了很久去怀疑「`--all` 环境里 ResizeObserver 是不是不投递」——
  其实红的那份是线上还没部署的旧代码，**断言报得完全正确**。
  单页那条路要显式给 URL，所以它天然是对的，两边的差异就是这个。
  用法提示行现在把 `--base` 写出来了，就是为了这个。
- **模板字符串（preview 脚本里那整页 HTML）的注释里不要写反引号**：反引号会把模板提前闭合，
  报成 `SyntaxError: Unexpected identifier`，而且指到的行离真正的错误很远。这个坑踩过两次
  （`locate-in`、`pointer-events: none`），照常写 `pointer-events:none` 就行。
  **第三次是 `drive-page.js` 里 `markShape` 那段**：注释里写了「音符自己的 \`d\`」，
  报的是 `SyntaxError: missing ) after argument list`。同一个坑，换个文件照样中。
- **同一个文件里 `\d` 和 `\\d` 是两件事，而写错了不会抛**：在 `evaluate()` 的那个模板字符串
  **里面**必须写 `\\d`（否则被模板字面量吃掉，页面拿到 `/d+/g`，什么都匹配不到）；在**外面**
  必须写 `\d`（写 `\\d` 会变成「反斜杠或字母 d」的字符类，同样什么都匹配不到）。
  两边都表现为「断言以一个和样式无关的理由失败」，而不是报错。
- **量「空格」的时候要把空格本身排除掉**：被折叠的空格仍然有一个 Range 盒子，只是宽度为 0
  并且正好贴在边界上 —— 拿它当邻居去量间距，**无论渲染成什么样都会量到 0**。
  要量的是「前一个可见字形」到「后一个可见字形」的距离（见 `drive-page.js` 里
  song-line join 那段的 `ink`）。第一次量的时候就是这么错的：before 报 0、
  after 报 −0.02，看起来像「两边都没有空格」。
- **「在动」要至少采三次才证明得了**：两次可能正好跨过一次重置（换行、回到起点），
  读到「没动」并误判成卡住。采三次、任意相邻两次前进即通过。
  只采一次实时的值，永远分不清「刚好停在边界上」和「边界根本没在动」。
- **要证明一条 CSS 动画「从小的长到大的」，去 CSSOM 里读它的 `@keyframes`**：
  在 `document.styleSheets` 里按名字（`rule.name` 含 `lyric-grow`）找 `CSSKeyframesRule`，
  读 `from` / `to` 两帧的 `transform`，断言起始 scale 小于结束的、且结束落在 1。
  `animation-name` 只证明动画**被应用**了，不证明它**做什么** —— 一个把行缩小的动画
  照样能通过「有没有动画」那一条。反过来，去采样实时的 `transform` 想一次证明两件事，
  两件都不牢：动画只在换行后 0.42s 内动，读晚了看到的就是静止值，
  而「样式表里根本没有这条动画」也长这样。
- **播放页的抽屉遮罩是 `position: fixed; inset: 0` 的 50% 黑**，所以开着抽屉截出来的图
  是隔着烟玻璃看歌词。要截歌词就先按 Escape 关掉抽屉 —— `scripts/probe-lyric-styles.js`
  对每个样式做一次「选中 → Escape → 截图 → 再打开」。
- **带入场动画的样式，截图只能证明其中一部分**：沉浸单行的长大是一个 0.42s 的动画，
  等它跑完截图什么也看不到 —— 要轮询当前行的文字，**在换行的那一瞬间**截。
  普通那一档，一帧就够。
- **截歌词行之前要先把它滚到中间，否则截到的是蒙版，不是样式**：`.lyrics` 有一层
  `mask-image`，把上下各 14% 淡到全透明。歌刚开始时「当前行」就是 LRC 的第一行，
  正好坐在这个淡出带上 —— 在那截出来的图里，任何明暗差别都被压到接近不可见，
  看起来像「样式根本没生效」。**几张图的 md5 各不相同，肉眼却完全一样**，
  这就是它难发现的地方：文件变了 ≠ 看得出来。做法是先把播放位置跳到曲中，
  再 `scrollTop = offsetTop - (clientHeight - offsetHeight) / 2`，等一帧再截
  （见 `scripts/probe-wipe.js`）。
- **`getComputedStyle` 在过渡途中返回的是「正在经过的值」**：沉浸单行的远处行是
  0.35s 淡出的，点完就量会读到「71 行全都可见」，看起来像样式根本没生效，实际是
  还没淡完。做法是先 `waitFor` 行自己的 `aria-checked` 变 true（它和样式类是**同一个
  commit** 上的），再睡过一个过渡时长，然后才量。别用固定 sleep 赌渲染时间。
- **事件派发和 React 重渲染不是同一轮**：`dispatchEvent` 之后**同步**读 DOM 拿到的是旧值，
  会把一个正常工作的方向键处理器报成「按了没反应」。中间必须隔一次等待
  （见 `drive-page.js` 里 arrow keys 那段的两次 evaluate）。
- **`prefers-reduced-motion` 可以用 `Emulation.setEmulatedMedia` 在冒烟测试里模拟**，
  用完 `{ features: [] }` 复位。值得单独断言：覆盖 `.lyrics-solo .lyric-active` 必须写
  两个类，写成 `.lyric-active` 会静默失效 —— 这种「看起来完全正确但不生效」的规则
  只有真跑一次才知道（变异验证时它确实红了）。
- **要断言「窄了才滚」，就把槽宽掐到标签两侧去，别指望某首歌刚好合适**：
  靠曲库里的歌去凑，读数会随曲库变，而且两条断言里总有一条是白过的
  （实测三条绿色读数里两条是 `159.9 <= 302`，什么都没证明）。做法是先量出 `ink`，
  再把它分别掐到 `ink + 10` 和 `ink − 10`：前者「放得下」只有修好了才对，
  后者「溢出」正好覆盖 `ResizeObserver` 那条路径。
- **两条断言里如果有一条的期望值正好是「什么都没发生」的默认值，就先问另一条**：
  「放得下时不要滚」的期望是 `false`，而 `false` 也正是「组件压根没测量过」的样子，
  所以先问它的话，即使组件完全不工作也会绿。改成**先掐窄（期望 `true`）、再掐宽
  （期望 `false`）**，两条就都从相反的答案出发，谁也白过不了。
  **期望值是默认值的那条断言，等于没写** —— 这条比上面那条更常见。
- **断言「等某个异步结果」要用 `waitFor` 轮询，不要睡固定时长**：`ResizeObserver`、
  过渡、重渲染都骑在渲染时机上，睡 400ms 是在赌这一帧来了没有。
  `waitFor` 的 `seen` 轨迹还顺便把「一直没变」和「变了又被改回去」分开 ——
  这是两种不同的 bug，报出来不一样才查得动。
- **`flex: 1` 的元素上写 `width` 是无效的**：basis 是固定的 0，所以掐槽宽必须同时写
  `flex: none`。只写 `width` 的话测试会以一个和被测代码无关的理由通过 —— 所以
  掐完要**回头断言槽宽真的变成了目标值**（`Math.abs(slot - forced) <= 2`）。

## 两套布局

```
pages/h5.js ───────▶ components/Music/h5/MusicApp.js ────────┐
                                                              ├─▶ components/Music/core/
pages/desktop.js ──▶ components/Music/desktop/DesktopApp.js ─┘   + Cover/Marquee/icons/
                                                                  shared/audioCache/librarySource
```

两棵树之间**没有任何 import**。`/h5` 里没有一行代码知道桌面端存在，反之亦然。
共用部分按职责分三块：

| `core/` 文件 | 是什么 | 为什么放这儿 |
| --- | --- | --- |
| `usePlayer.js` | 全部播放状态：曲库、缓存、歌词、Google、主题、抽屉开关 | 两套布局要共享**行为**，且必须逐字一致 |
| `PageHead.js` | `<Head>` 标题 + GSI `<Script>` | 两个页面都要有同样的 title；GSI 只给真的会去连云盘的布局加载（`gsi={false}` 的页面连那个库都不下载） |
| `PlayerAudio.js` | 那唯一一个 `<audio>` | 六种 handler 由 `usePlayer` 统一返回，少接一个就是「进度条永远不动」且不报错 |
| `CacheContent.js` | 缓存管理的**内容** | 曾经两套布局共用它（底部抽屉 / 玻璃卡片），现在只剩手机端一个外壳 —— 桌面端那份面板随设置弹窗一起删了 |
| `MarkNote.js` | 应用图标，也是进「账号」的按钮 | 它是 **logo**，不是某一套布局的控件：两棵树各画一个就是两个 logo。调用方只能改它的盒子（`className`），改不了它的画 |
| `sheetBase.module.scss` | `.body` / `.state` | 面板内容共用的滚动容器与加载文案 |

**面板一律「内容 + 外壳」两半**：内容在 `core/`，外壳各自实现（`h5/SheetChrome.*` 是底部
升起的抽屉）。桌面端那一半**已经没有了** —— 它的设置弹窗连同 `desktop/DesktopSheetChrome.*`、
`desktop/DesktopCachePanel.js` 一起删掉了，所以 `core/CacheContent.js` 现在只剩手机端一个外壳。
**「缓存」这个功能本身没动**：缓存在 `usePlayer` 里，照常下载、照常续期，只是桌面端不再有
能看它的面板。
内容组件返回的是 **Fragment**，因为外壳是个 flex column，它那几块要当直接子节点才能保住
`flex-shrink: 0` / `flex: 1`。

`usePlayer` 返回一个扁平的 ~80 字段对象（而不是拆成几个小 hook），就是为了让这次拆分
能**逐字搬迁**手机端的行为 —— 拆 hook 会顺手改掉依赖数组和执行顺序。

`lyricsAutoOpen` 是两套布局**唯一**真正分歧的地方：桌面端的舞台就是歌词，
有歌词就展开是对的；手机端则会平白盖住自己的列表，所以传 `false` 再自己接管。

桌面端列表还有一条手机端没有的规则：**有歌在放、鼠标又不在列表上，3 秒后列表自己收起**
（`LIST_HIDE_MS`）。它折的是 `autoHidden`，不是 `listOpen` —— 后者是访客的选择、
是要存进 localStorage 的那个；前者只是播放器替访客做的临时决定，所以暂停、搜索、
开着菜单、鼠标回到列表上都会立刻把它放回来。写这个 effect 时依赖必须是**布尔量**
（`hasCurrent` 而不是 `current`）：`current` 每次渲染换身份的话，定时器会被反复
清掉重设，列表就永远不会收起。

**`current` 不是一首歌，是 `{ track, url, startTime, shouldPlay }`。**
歌在 `current.track` 里 —— 要写 `current.track.name`、`current.track.id`、
`coverUrlOf(current.track)`。判断「有没有歌在放」用 `Boolean(current)` 就对了，
一旦要**读字段**就必须往下走一层。这一条被踩过一次：
有一棵树写成了 `current.name` / `current.id` / `coverUrlOf(current)`，
于是点列表里任何一首歌都会**把整页打崩**（`parseTrackName` 第一行就是
`name.match(...)`，拿到 `undefined` 就抛；React 19 渲染期抛异常会卸掉整棵树，
只剩错误边界那张卡片），同时唱片标签永远不上封面、正在播的那一行永远不高亮。
三个症状一个原因，改完三处一起好。

**改一套布局时先想另一套**。以前这个缝漏过三次：`置顶` 只接在手机端；
桌面端用 Google token 而不是 `hasLibrary` 判空状态，导致公共曲库明明加载好了却显示
「曲库里还没有歌曲」；`回到正在播放` 长在手机端迷你条上，桌面端根本没有。
现在两套是独立文件，漏了更不会报错 —— 只是那一套「没有这个功能」而已。
（反过来的例子也在：手机端顶部栏里那个头像占位**只有手机端有**，那是设计，不是漏。）

### 桌面端的毛玻璃

**纯 CSS，没有 WebGL，没有玻璃库**（`@ybouane/liquidglass` 试过两次，都卸了，
见下方迁移表）。一个「玻璃面」就是三件事：半透明填充 + 发丝描边 + `backdrop-filter`。

**它是平的，不是立体的**。这里曾经有第四件事 `--glass-inset`
（`inset 0 1px 0 rgba(255,255,255,…)`，每个面的上沿一条亮线）和 `--glass-shadow`
里那层紧贴的 `0 2px 10px -4px`。那条亮线就是「立体感」的来源 —— 一个被照亮的盖子，
读成凸起的塑料按钮，而不是一块磨砂玻璃。两个都已删除，`--glass-shadow` 只剩一层
宽而淡的环境投影（只是把面和背后的色场分开，不描边）。**加回任何内阴影 / 顶高光
都会把这个观感带回来**，要加请先确认。

token 只在**一处**声明：`desktop/DesktopApp.module.scss` 的 `.page`。
`DesktopMusic.module.scss` 以及 `core/` 的内容组件全部只**消费**、不声明 —— 一个定义，工作台和外壳就不可能各走各的。

**桌面端只有深色一套。** 这里原本是 `.page`（浅色）+ `.page.theme-dark`（深色）两个块，
设置弹窗（现已整个删除）的「外观」组里有一行 `切换到浅色 / 深色模式`。那一行和浅色值一起删掉了：
`/desktop` 不再读 `usePlayer` 的 `theme`，手机端那个开关也就管不到这个页面。
**加回浅色模式 = 重新引入一整套 token + 一个 modifier 类 + 一个入口**，不是加一行。

| token | 用途 |
| --- | --- |
| `--glass-blur` / `--glass-sat` | `backdrop-filter: blur() saturate()` 的两个参数 |
| `--glass-bg` | 常规面：胶囊播放条、折叠后的列表开关 |
| `--glass-bg-soft` | 玻璃**之上**的凹陷（搜索框、输入框） |
| `--glass-bg-strong` | 要压住繁忙内容的面：列表面板卡片、toast、定位按钮 |
| `--glass-border` / `--glass-shadow` | 描边、环境投影 |

`.backdrop` 是**独立的兄弟层**（不是 `.root` 自己的背景）：`backdrop-filter` 只采样
它**背后**已经画好的东西，把四团色晕放在自己那一格里，模糊才有东西可糊。
`.glow` 是它上面再一层、用当前歌曲的 `trackGradient` 做的圆形色晕（`mask-image`
抠出来的软边，不是 `filter: blur()` —— 同样的边缘，一次绘制，没有离屏大缓冲）。

**不支持 `backdrop-filter` 的浏览器**由 `DesktopApp.module.scss` 里一个
`@supports not (…)` 兜住：它只把三个 `--glass-*` 的 alpha 提到接近不透明，
布局、描边、投影一律不动 —— 所以没有任何东西会移位，只是不再透。
那段必须和 token 声明在同一个文件里，否则会被 `.page` 按源码顺序盖掉。
现在只剩深色一套，`@supports` 里也就只剩一个块。

### 桌面端：舞台占满全屏，别的东西都浮在上面

```
.root  (100vh，overflow: hidden —— 整页不滚动)
├── .backdrop / .glow          色场，铺满
├── .stage  (absolute; inset: 0)   ← 唯一有自己布局的块
│     ├── .stage-record         唱片（有歌词时 opacity: 0，不卸载）
│     └── .lyrics               歌词，absolute 铺在 stage 上
├── .side    (absolute)  列表：无面板背景，直接滚动
└── .bar     (absolute)  胶囊播放条
```

（这里曾经还有一行 `.settings-btn (absolute)` —— 右上角那个齿轮。它和它打开的整个设置
弹窗都已删除，见下面「桌面端只有公共曲库」。）

**这是桌面端最容易改坏的一条约定**：舞台是 `position: absolute; inset: 0`，
其余全是它的**兄弟**、绝对定位在它上面 —— 所以折叠列表、开行抽屉、切歌词
都不可能让唱片挪一个像素。想让某个新控件「浮着」，就绝对定位；一旦把它塞进
`.stage` 的流里，它就会开始推唱片。

几条随之而来的硬约束：

- **`.root` 不能有 `transform` / `filter`**：那会让 `position: fixed` 的
  面板 / 弹窗以它为包含块，`overflow: hidden` 就会把它们裁掉。
- **`.side` 是 `pointer-events: none`**，只有里面的开关和 `.panel` 是 `auto` ——
  否则列表那一列空白会把本该落到唱片上的点击吃掉。
- **胶囊播放条的几何写在 `.root` 上**（`--bar-h: 72px` / `--bar-bottom: 26px`），
  因为 `.side` 的底边要用它来让开播放条。改一个数字，两处一起动。
- **`.bar-row` 的 padding 是故意不对称的**（`24px 30px 8px`）：上边 24px 是给进度条
  让出来的带子，下边 8px 才是行自己的余量。`24 + 40 + 8 = 72 = --bar-h`，
  行正好占满剩下的高度。它曾经是 `padding: 0 30px 11px`（进度条骑**下沿**那一版，
  行心被抬到离胶囊底 37.5px），进度条挪到上沿时才翻过来。
  **代价是整行落在胶囊中线下面 8px，这是有意的** —— 有人（包括我）看过之后
  以为「播放键没居中」是指整行偏下，把 padding 改成上下相等，结果进度条被挤回行里、
  还要再动 `.seek`。**「播放键没居中」指的是图标在按钮里偏心（当时那枚还是实心红圆），
  那是 `.ctrl-play` 的事，和这个 padding 无关。**
- **`.seek` 绝对定位在胶囊内部**（贴着上沿、左右各让开 30px）：`top: 6px` 让 3px
  轨道的中心落在离胶囊顶约 12.5px 处，12px 滑块整个待在行上边距那条带子里 ——
  所以拖到哪儿都不会碰到控件。左右各让开 30px 是因为胶囊圆角 999px，
  行内边距小于半径时内容会跑到填充外面去。
  - 已播的那半是 **`--progress`（中性白）**，不是 `--accent`：一条 3px 的红线横贯
    整个胶囊是这条里最响的东西，播放键去掉红底之后它就是最后一点红。
    滑块保持 `--text`（白），那是抓手，不是进度。
- **胶囊条是唯一没有描边的玻璃面**（`.bar` 上没有 `border`）：别的面都带
  `--glass-border`，而它的边是半径 36 的半圆，1px 发丝线在那个形状上读成
  「围着胶囊画了一圈铁丝」，不像一块玻璃的边。边由填充和那层环境投影自己撑起来，
  不要顺手把它统一回带描边的样子。
- **`.ctrl-play` 的 `padding` 是 0**：它抄过手机端播放键的 `padding-left` 光学微调，
  那是给**透明**按钮用的，桌面端当时是一枚实心红圆 —— 同样的 1px 在圆里是量得出来的。
  `IconPlay` 的 path 自己已经带了偏心（墨迹中心在 viewBox 中心右边 2.17 单位，
  右指三角看起来才正），`IconPause` 本身对称，所以居中盒子 = 两个图标都正。
  （那枚红圆现在也没有了，见下面「播放 / 暂停键没有底」。）
- **唱臂的盒子按「手机端的坐标系相对唱片」算，不是按眼睛调**：
  `.arm` 是 `width: 116.28%; height: 141.86%; top: -20.93%; left: -8.14%`。
  这四个数全部来自手机端 —— 那边 `.rig` 的 `aspect-ratio` 是 `100 / 122`
  （**就是唱臂 SVG 的坐标系**），唱片是 rig 宽度的 `86%` 且居中；桌面端反过来，
  rig 就是唱片，所以唱臂的盒子得是 `100 / 86` 宽、`122 / 86` 高，再往左上
  各推 `(1.1628 - 1) / 2 = 8.14%` / `(1.4186 - 1) / 2 = 20.93%`。
  这样两边「一个 SVG 单位 = 1/86 张唱片」，**共用的那段 path 才真是同一个物体**。
  它曾经是 `width: 46%`（按眼睛调的覆盖层），于是桌面端的唱臂比手机端**小 2.5 倍** ——
  一枚玩具般的唱臂趴在唱片上，转轴只有豌豆大。改这个数之前先想清楚：
  它同时决定唱臂、唱针和转轴三者的尺寸（同一个 `viewBox` 等比缩放）。
  盒子变大不会挤到别的东西：它画出来最左的东西是转轴的光晕（`0.47` 倍唱片宽），
  最低的是唱针（落在唱片右上缘 `0.98` 半径处，和手机端同一点），
  多出来的面积几乎全是空的。旋转中心不受影响 ——
  `.arm-swing` 的 `transform-origin: 52.5% 3.77%` 是 `view-box` 单位，与盒子大小无关。
- 列表的 `.list` 用 `mask-image` 在**两端**渐隐，而且两端的长度是**变量**
  （`--fade-top` / `--fade-bottom`，默认 `0px`），由 `DesktopMusic` 的 `listEnds`
  按滚动位置设成 `36px`：面板没有边框，硬切会被读成「面板被裁了」，
  而渐隐只有在那一头**确实还有内容**时才该出现 —— 短到不用滚的列表两端都不渐隐，
  滚到底之后下沿也不再渐隐。一条渐变 + 两个长度，四种状态（都不 / 只上 / 只下 / 都）
  只差数字；`0px` 时停靠点重合，本身就是 no-op，所以默认状态不需要额外规则，
  也不需要 `mask-image: none` 去撤销。
- **行的 hover 底色长在 `<li>` 上**（`.track-row` / `.track-row-active`），
  不在 `.item` 上：右侧的三个点是行的一部分，底色只铺到播放按钮就停下，
  会被读成「两个控件」。`.track-row-active` 必须声明在 `.track-row:hover` **之后**
  （两者同权重），并且要自己写 `:hover`，否则悬停中的播放行会掉回 `--hover`。
  **`.track-row-active` 是「加到 `.track-row` 上」，绝不能替代它** ——
  这里踩过一次：`<li>` 曾经二选一地挂一个类，而布局（`display: flex`、居中）
  全在 `.track-row` 上，于是正在播放那一行退化成普通块级盒，
  播放按钮撑满整宽、三个点被挤到第二行，**整行比别的行高一倍**。
  随之而来的：`.item-more:hover` **只改颜色、不填底** —— `--hover` 是半透明的，
  再填一次会把同一层淡色画两遍，行里会出现一块明显更深的方块。
- **底色没有圆角，两端是渐隐的**（`.track-row::before`）。它曾经是 `border-radius: 14px`
  的一块 `--hover`，读起来像一枚贴在色场上的**卡片** —— 四条边都要被看见，而这个列表的
  全部意思就是「一行是一行字」。现在它和行一样宽，两端各 22px 淡出到透明，
  指针说的是「这一行」，不是「这个盒子」。
  - 遮罩只能裁**底色**，不能裁行，所以它是一层 `z-index: -1` 的 `::before`，不是挂在
    `<li>` 上的 `background`（挂在 `<li>` 上，封面和歌名会跟着一起淡）。
    负层漏不到页面背后：`.side` 带 `z-index: 6`，本身就是一个层叠上下文。
  - `.track-pulse`（跳到正在播放那一行之后闪一下）跟着改成动画这一层的 `opacity`，
    而不是改 `<li>` 的 `background` —— 否则一次带圆角的闪光就把这个遮罩的意义抹掉了。
- **列表那一列顶部是一行 36px 方块**（`.side-head`）：应用图标 `.side-mark` 独占左端，
  右边是三个列表控件 —— 搜索（关着的 `.search-btn` / 展开后的 `.search`）、
  只看喜欢 `.side-heart`、折叠开关 `.side-toggle`。四个都是 36px、圆角都是 12px，
  所以开合搜索时列表一个像素都不动。
  - **图标在左、操作在右**，中间那段空档由**搜索那一格自己的 `margin-left: auto`**
    花掉（`.search-btn, .search`）。手机端顶部栏是同一种排法：图标在左、操作在右、
    输入框在中间长开。**这条 margin 不能挪到别的方块上**：两个 `auto` 会把空档分成
    两半、一组就散了；整个去掉，三个方块又会挤在图标旁边。
  - **搜索必须排在这一组的第一个**：它是唯一会变成 `flex: 1` 的那一个（点开就是输入框），
    而 `flex: 1` 只会推开**它后面**的东西。它领头，输入框就只往左长进那段空档，
    右边两颗（红心、折叠）一个像素都不动 —— 红心排在它前面的话，输入框一开
    就会把红心一路顶回图标旁边。
  - 输入框的 `transform-origin` 因此是 **`right center`**：它右边的位置就是那个按钮
    原来待的地方，长出来的是它的左边。
  - 图标就是 `core/MarkNote`（见上面「应用图标」那条），传 `side-mark` 把盒子改成 36px、
    去掉手机端那个 6px 内缩，和列左边缘齐平。**它是这一行唯一一个「不是关于列表」的控件**，
    所以折叠时它不走（下面那条）。
  - **只看喜欢沿用手机端 `nav-btn-on` 的漆**（`--accent` 字 + `--row-active` 底），
    同一个控件在两套布局上长得一样。列表这一层不管「一个红」这条：那是**播放条**的
    规矩，而且那条现在更紧了 —— 整条一个红都不留（见 `.ctrl-on` 与 `.ctrl-play`）。
    这里的红心是列表唯一的筛选，开着就该从房间那头看见。
  - `.side-head` 和 `.side` 一样是 `pointer-events: none`，只有这四个控件是 `auto`：
    行里没被占满的那段空档是悬在唱片上的空白，不能吃掉点击。
  - 折叠时**搜索和红心**跟着列表一起走（`.side-folded .search-btn`、
    `.side-folded .side-heart`、`.side-folded .search`，用 `visibility` 而不是
    `display`，行高不变、开关不移位；`.search` 上不能用 `opacity`，因为 `search-open`
    动画的 fill 优先级高于普通声明）。两个都是筛列表的东西，列表不在就都该走；
    **图标留在外面**，它通向「账号」，那是访客自己的事。
  - 三个方块共用一条基础规则（`.side-toggle, .side-heart, .search-btn`）—— 它们是一行，
    两个方块差 2px 就会被读成失误；折叠开关自己的玻璃底只写在 `.side-folded .side-toggle`。
  - **折叠开关的图标必须分状态**：展开时 `IconPanelFold`（雪佛龙头朝左，
    「收起来」），折叠时 `IconPanel`（朝右，「拿出来」）—— 两个是同一枚图标的镜像，
    形状不同会被读成两个控件。原来两种状态共用一枚图标，那个按钮就没法说明自己要往哪走。
  - 列表头上那个三点弹出菜单（`.menu-*`）**已删除**：它的几项都搬进了右上角设置弹窗。
    （其中「不喜欢歌曲」后来连同它所在的整个「曲库」分组一起删掉了 —— 见上面「列表偏好」一节。
    再后来**那个设置弹窗也整个删掉了**，所以这些项现在哪儿都没有 —— 见下面「桌面端只有公共曲库」。）
- **唱片下方不再有歌名 / 歌手**（`.head*` 四条规则已删除，`.stage-record` 现在只装唱片）。
  理由不是「简洁」，是**同一首歌在页面上被说了三遍**：列表行、唱片下方、胶囊条，
  而唱片下方那遍最响、却既点不动也拖不动。要再放东西到唱片下面，
  先确认它不是在重复别处已有的信息。`.stage` 的 `padding-bottom: 12vh`
  与 `.rig` 的 `56vh` 都还留着 —— 前者现在负责让唱片避开播放条，
  后者当初是按「唱片 + 标题」算的，标题没了就有余量，但 56vh 是唱片被调好的尺寸，
  不要因为「有余量」就顺手放大。
- **底部胶囊条里的唱片是回来了的**（`.bar-disc`，34px）：它被删过一次，理由是
  「舞台有真的唱片、行里有封面，这是第三份，在这个尺寸上只是一枚带糊图的深色圆点」。
  让它回来的不是那半句被推翻，而是另一半：歌词（现在是默认）一上屏，舞台那枚唱片
  就 `opacity: 0` 了，行里的封面也随列表滚走 —— 只有这一枚始终在屏幕上。
  尺寸是手机端迷你条的比例缩下来的，**两边画的是同一个物体**。
- **播放 / 暂停键没有底**：40px、`background: transparent`、字形是 `--text`
  （旁边四个是 36px、字形 `--text-2`）。它一路减配过来：先是
  `linear-gradient(accent-2 → accent)` + `0 12px 26px -10px` 的红色光晕（一颗被照亮的
  凸起红球，全站刚删掉的那种立体感），再是平的 `--accent` 圆，现在是**什么都不填** ——
  进度条的填充也不再是红的，一枚红圆就是整条里最后、也是最响的一点红。
  主按钮的身份改由**颜色**承担：它的字形是 `--text`，邻居是 `--text-2`，再借运输控件
  共用的 hover 软底。**40px 的盒子留着**（虽然没底就看不见了）：它是这一行里最高的
  东西，`.bar-row` 的内容高度就是按它量的，改小会把运输控件挪位。
  手机端的播放键是 30px 装 18px 图标（图标撑满按钮），桌面端是同一个 18px 图标
  配 40px 的余地。
- **条里的字号**：歌名 14px（手机端迷你条在 58px 高里用 13px），时间 12px。
  胶囊矮下来之后字号必须跟上，否则会显得空。
- **喜欢那颗心在条尾，和时间挤在一格**（`.bar-end` = 红心 + `.times`）：
  它没放在歌名旁边（手机端的播放页把它放在那儿），因为**歌名那一格在 1180px 以下
  整个 `display: none`** —— 一台小笔记本上会消失的喜欢按钮，比一个挨着时钟的喜欢按钮更糟。
  右格变宽不会把播放键挤偏：这一行是 `1fr auto 1fr`，两侧格子永远等宽，中间那格才是
  运输控件，它始终对着**胶囊**居中。
- **红心借的是运输控件自己的类**（`.ctrl-btn` + `.ctrl-on`），没有自己的规则：
  同一个 36px 圆环、同一套 hover、同一个「开」——亮字 + 一层软底。
  **故意不涂 `--accent`**，而手机端播放页里那颗被喜欢的心是涂红的：这条胶囊里
  **一个红都不留**（播放键没底、进度条的填充是 `--progress`，见那两条），
  一颗实心的红心就会是它唯一的一点红，把整条变回「一颗药里两套配色」。
  实心字形加那层软底已经把「已喜欢」说清楚了。
- **滚动条默认隐形，hover / focus 才显形**：这条规则现在只挂在 `.list` 上
  （它曾经和 `.settings-body` 并列，那个设置弹窗删掉之后就只剩这一处滚动了）。
  `scrollbar-width: thin` +
  `scrollbar-color: transparent transparent`，`:hover` / `:focus-within` 时换成
  `var(--track)`，同时把 `::-webkit-scrollbar-thumb` 的 `background` 一起换掉
  （两套写法都设成同一行为，因为引擎可能认标准属性、也可能认 `::-webkit-*` 伪元素：
  Chrome 的几何取自 `scrollbar-width`、绘制取自伪元素）。
  关键是 **`thin` 而不是 `none`**：滚动槽一直占着那 10px，所以显形时一行都不会横移
  （实测两种状态下 `row item x 18 → 288` 完全一致）。hover 那半放在
  `@media (hover: hover)` 里（触屏上 `:hover` 会黏在最后点过的东西上），
  `:focus-within` 不在里面 —— 键盘没有指针。
- 行按钮（`.item` / `.item-active`）的 `:focus-visible` 用 `outline-offset: -2px`
  （**内缩**）：行是紧挨着排的，外扩的环会画到上一行身上。
- **唱片的宽度要自己让开列表那一列**：
  `min(56vh, 44vw, 700px, max(300px, calc(100vw - 2 * var(--side) - 56px)))`。
  `44vw` 不够 —— 它量的是整个视口，而唱片是居中在视口里的，所以「窄而高」的窗口
  （1920×1080 屏左右分屏后就是 960×1040）会让 `56vh` 把唱片撑到左缘滑进列表下面
  （900×1000 实测 −19px）。第四项按 `--side` 在两侧各留一列，保证行与唱片之间
  恒有 40px；它在 ~1340px 宽以上不会生效，所以常见尺寸一点没变。
  外面那层 `max(300px, …)` 是**下限**：第四项在 616px 宽时会归零，
  而一枚缩到看不见的唱片比一枚稍微探到列表下面的唱片更糟。下限在 ~880px 宽以下
  才开始生效，已经在 `pages/index.js` 把访客送去 `/h5` 的 900px 之下 ——
  所以桌面布局真正服务的每个宽度都还是正间距（900×1000 是 32px）。

### 桌面端只有公共曲库

右上角**没有齿轮、没有设置弹窗**了。那个弹窗里的东西要么是「连接自己的云盘」，
要么是唱片波纹，而这两样这个布局现在都不做。跟着删掉的是三样：
`desktop/DesktopCachePanel.js`、`desktop/DesktopSheetChrome.{js,module.scss}`
（它们只被那个弹窗引用，没有别的进口），以及 `DesktopMusic.module.scss` 里那 244 行设置样式。
判断哪个组件是死的，用**谁 import 它**，不要用「名字里有没有 desktop」猜 ——
`core/CacheContent.js` 看着像桌面端的，其实是手机端 `h5/CacheManager` 在渲染它。

「只播公共曲库」不是把 Drive 的代码删掉，是 **`usePlayer({ drive: false })`**。
`usePlayer` 里唯一一处「不用点一下就切到云盘曲库」的地方就是那个 token 恢复 effect，
所以只要它 `if (!drive) return;`，client id 就不会被读出来 —— 没有 client id 就没有 token，
没有 token 就 `librarySource` 一直停在公共曲库，而下面每一条 Drive 路径本来就守着这两个之一。
**client id 本身还是照旧恢复**，因为公共曲库的清单缓存也拿它做命名空间
（`readListCache(CLOUD_SOURCE, savedId)`）。云盘那套状态、`connect` / `disconnect`、
`fallbackToPublicLibrary`、过期监听全都还在 `usePlayer` 里 —— 在 `/desktop` 上只是永远走不到，
手机端一行没动。**要真删就得两棵树一起改，那是另一件事。**

`PageHead` 跟着多了个 `gsi` 参数（`/desktop` 传 `gsi={false}`）：title 和 description 每页都要，
但**授权库不是** —— 一个连不上云盘的页面不该去下载 GSI。

**唱片不再有波纹，这个布局也没有这个设置**：那三个 `<span className={styles.ripple} />`
是桌面端自己画的，偏好却是和手机端共用的那一个 `ripples`。JSX 连同 `.ripples` / `.ripple` /
`@keyframes ripple-out` 一起删掉之后，这个布局就没有它的消费者了。
**手机端播放设置抽屉里那一项还在**，它从来是另一处开关（`h5/NowPlaying.js` 自己的抽屉），
这次动的只是 `/desktop`。

`emptyListMessage` 跟着多了一个分支：**没有 `folderHint` 就没有后半句**（只说
「公共曲库里还没有歌曲」）。桌面端没有文件夹选择器，也没有第二个曲库可换，
「去『音乐库』换个文件夹试试」在这里是指向一个不存在的地方的一句话。
`likedOnly` 现在也传进去了，否则「只看喜欢」筛空时会说成「曲库里还没有歌曲」。

### 桌面端的喜欢与账号

**喜欢现在是两套布局都有的功能**，桌面端补上的三处：

| 位置 | 是什么 |
| --- | --- |
| 列表顶部那颗心（`.side-heart`） | **只看喜欢**：筛列表，亮着时是手机端 `nav-btn-on` 的漆 |
| 行抽屉第二项 | **喜欢 / 取消喜欢**，和手机端同一个抽屉里、同样的两行、同样的顺序 |
| 胶囊条尾部那颗心（`.bar-end` 里的 `.ctrl-btn`） | 当前这首歌的喜欢状态，点一下切换 |

**没有一处判断 `canLike`**：手机端用它挡住云盘曲库的歌（云盘的文件 id 出了那个账号就没有
意义），而 `/desktop` 只播公共曲库，那个条件在这里永远是 true。
**喜欢也不需要一个 QQ 号**：没确认号码时它存在这台设备的浏览器里（`likes.js` 的 `guest` 桶），
确认之后 `adoptGuestLikes` 把它们收进号码下 —— 抽屉的副标题就是照这个写的，
两种情况说的话不一样。

**账号是一张居中的卡片**（`DesktopApp.js` 的 `accountOpen` + `DesktopApp.module.scss` 的
`.scrim` / `.card`），和行抽屉共用同一套外壳：宽屏上「从底边升起」会离打开它的东西太远。
卡片里只有一样东西 —— **QQ 号**。手机端那张 账号 是访客的整个面板（号码 + 听歌排行 +
数据同步），这张**故意不是**：后两个屏幕在这个布局里不存在，而一张带着「通往一个打不开的
屏幕」的行的卡片是个死胡同。

- 和手机端一样是**一块、两种状态**：有号码就是身份卡（头像 + 号码 + 号码是干什么的）
  和一个「清除」，没号码就是输入框 + 「确认」+ 提示。同一时刻只有一块在屏幕上 ——
  「我的号存上了没有」这个问题曾经有两个半截答案同时在屏幕上（卡片、输入框的占位符、头像），
  手机端为此重写过，这张一上来就是这个形状。
- **头像是从 `usePlayer` 拿的**（`avatarUrl` / `onAvatarError`），不是各壳自己算的：
  它是号码的函数，而号码就在这个 hook 里（理由是同一个 —— 一个壳自己算一次，就是两个
  可能不一致的兜底）。这段逻辑原来住在 `h5/MusicApp`，那时只有手机端有身份卡。
- 「确认」是**平的 `--accent`**，不是手机端 `primary-btn` 的
  `linear-gradient(135deg, accent-2, accent)`：那个渐变就是这一页到处都删掉的立体感，
  播放键平掉的理由和它一模一样，为一个按钮把它请回来不值。
- `MarkNote` 的角点就是**同一个 QQ 状态点**（灰 / 绿），所以「我的号存上了没有」在列表上
  一眼可见，不必先打开卡片。
- 号码的校验只有一个地方 —— `shared.js` 的 `normalizeQq`。**两个入口、一条规则**：
  两处各写一遍「什么算一个号码」，迟早会一处收下另一处不收的东西。

## 迁移时替换了什么

| 原（space 博客） | 现（本仓库） |
| --- | --- |
| `components/SEO`（依赖 i18n + 站点配置） | `core/PageHead.js` 里直接用 `next/head` 写 title/description |
| `config` 里的 `site` + `supportedLanguages` | 只保留 `site` + `music`，`title` 改成 `Music Space` |
| `shared.js` 里的 IndexedDB 代码 | 拆到 `lib/cache/indexedDb.js` + `components/Music/audioCache.js` |
| 挂在 `pages/music/` 下 | 改成根路径 `/`、`/h5`、`/desktop` |
| `utils/basePath.js` | **已删除** —— 唯一调用者是 SW 注册，SW 移除后无人使用 |
| 一个 `MusicApp` + `variant` 分支渲染两套布局 | 拆成 `h5/MusicApp` 与 `desktop/DesktopApp` 两棵独立的树，共用 `core/` |
| `desktop/DesktopCachePanel.js` + `desktop/DesktopSheetChrome.*` | **已删除** —— 它们只被桌面端的设置弹窗引用；那个弹窗（连接云盘、缓存管理、外观、唱片波纹）整个删掉了，因为 `/desktop` 现在只播公共曲库、也没有波纹 |
| `@ybouane/liquidglass`（WebGL 玻璃） | `d2cd9fa` 时**已卸载** —— 桌面端全部改成纯 CSS `backdrop-filter`；`7921a1c` 起又给底部播放条装回来试过一轮，`cfae6bd` 之后**再次卸载**（原因见那条提交：背后没有可折射的结构、72px 高的条上默认倒角 40px 把整条变成鼓包，所以怎么调都不像 demo） |

## 常用命令

- `npm run dev` — 本地开发
- `npm run build` — 构建，产物在 `out/`
- `node scripts/preview-desktop-list.js` — 生成列表面板的可量尺寸预览页（先 `npm run build`）
- `node scripts/preview-covers.js` — 生成封面的可量尺寸预览页：列表/抽屉/缓存/唱片四种形状，每种都放了「有封面」和「没封面」两个对照
- `node scripts/preview-empty-list.js` — 生成空列表文案的预览页：四个分支两套布局并排，另附一列「旧写法（`<p>` 在 `<ul>` 里）」对照，量「消息是不是列表的兄弟节点、有没有真的画出来」
- `node scripts/drive-page.js <url>|--all [--insecure] [--track=X] [--size=WxH] [--out=前缀]` — 用真 Chrome 打开页面、点一遍、按断言判成败，并报告失败请求和全部异常；有异常或断言不过就非零退出。零依赖（Node 22 自带 `WebSocket`，直接说 DevTools 协议）。`--all` 跑两棵树（`/desktop/`、`/h5/`）
- `node scripts/serve-static.js <根> [端口]` — 零依赖静态服务器，给下面那套「截图看一眼」用：目录请求回 `index.html`，目录里只有一个 html 时回那一个。这台机器的 python3 是个装了一半的 3.13（`Failed to import encodings`），所以用它顶 `python3 -m http.server`
- `node scripts/probe-lyric-styles.js [--track=夜曲] [--prefix=fx]` — 四种歌词样式各截一张图，真 Chrome，裁到字上（先起 `serve-static.js`）
- `node scripts/probe-wipe.js [--track=夜曲]` — 把 `--wipe` 钉在四个已知值各截一张，量卡拉OK 填充是不是真的两段色（先起 `serve-static.js`）
- `node scripts/probe-offline.js [--port=8913] [--chrome=PATH]` — 离线外壳（`public/sw.js`）的回归检查：装完有没有接管、缓存里有没有外壳 HTML 和 `_next` 资源、**在线时会不会误读缓存**、**断网时是不是真的回缓存**、激活时的清理有没有动别人家的缓存。自带静态服务器（把 `out/` 挂在 `/music/` 下），`npm run build` 之后直接跑，不用 Junction、也不用先起 `serve-static.js`
- `cd cloudflare-worker && npx wrangler deploy` — 部署曲库 Worker

### 要看「画出来是什么样」的时候

改玻璃、布局这类**视觉**的东西，`npm run build` 只证明语法没坏。
要真的看一眼，别去量无头浏览器的 DOM —— 用 Junction 搭一个带真 basePath 的
本地 HTTP 根，再截图：

```bash
# 1. 搭一个只放 Junction 的根目录（在仓库外，用完直接删）
#    别搭在仓库里：`rm -rf` 一个含 Junction 的目录会顺着链接删进源码
mkdir -p /d/tmp/serve && cd /d/tmp/serve
cmd //c mklink /J music "D:\code\music\out"   # music -> out
                                              # 于是 http://127.0.0.1:8899/music/h5/ 是真应用
# 2. 起静态服务器（二选一，见下）
python3 -m http.server 8899 --bind 127.0.0.1 --directory /d/tmp/serve
node scripts/serve-static.js /d/tmp/serve 8899
# 3. 截图
chrome --headless=new --window-size=1440,810 --timeout=25000 \
       --screenshot=shot.png "http://127.0.0.1:8899/music/h5/"
```

这台机器上的 python3 是个装了一半的 3.13（`Failed to import encodings`），基本用不了，
所以实际都用零依赖的替身 `node scripts/serve-static.js <根> [端口]` ——
它多做两件事：目录请求回 `index.html`，目录里只有一个 html 时回那一个。

**截图时进场动画可能停在第一帧。** `MiniPlayer` 那类带 `animation: … both` 的元素，
在只被截一次图的页面里可能停在 0% 关键帧上（`.wrap` 的 `translateY(18px)`、
`.locate-btn` 的 `translateY(8px) scale(0.9)`），于是量出来的位置和尺寸都是错的 ——
`scripts/preview-locate-btn.js` 为此在自己的样式里显式 `animation: none`。
**logo 的音符也是这一类了**（`note-in` 的 `1s both`，0% 那一帧是 `opacity: 0`），
所以单独截 h5 页面有可能拍到一个「没有音符」的 logo —— 而且现在是**两层**一起淡入
（`.art` 和 `.pane`），所以拍到的是「既没有音符也没有那块玻璃」。`scripts/preview-mark.js`
里每个 cell 都显式冻结了动画（两层都要冻），正是为此。量之前先确认那两条读数是「布局的」
还是「第一帧的」。

三个坑，踩过就别再踩：

1. **`file://` 下 ES module 动态 import 一定失败**（不透明 origin），必须走 HTTP。
2. **页面里有活的 rAF 时 `--virtual-time-budget` 永不耗尽**，`--screenshot` /
   `--dump-dom` 会一直挂到超时。真页面用 `--timeout=N`；自己写的 harness
   则把循环写成有限步（跑 N 帧、渲染一次、停）。
3. **importmap 要补没有扩展名的裸相对导入。** `scene/lyrics.js` 里写的是
   `from './textures'`，浏览器不做扩展名补全 → 404，但报出来的错是
   `Failed to fetch dynamically imported module: .../lyrics.js` ——
   错误指向最外层那个模块，不是真正 404 的那个，很容易查错方向。

`scene/index.js` 是**可以脱离 React 一帧一帧驱动的**（`frame(delta, state)`），
所以除了单模块 harness，还能搭一个只 import `createStage` 的页面，
自己喂 200 帧 `{ playing, hasTrack, progress, lyricsVisible, lyrics, lyricsTime }`，
就能在无头浏览器里看到**完整构图**（唱片 + 唱臂 + 尘埃环 + 歌词）——
不用起 dev server，也不用等一首真歌。要看擦除在哪一刻是什么样，
就按时间跑（`?t=7.4`）而不是按帧数跑。

**判断「差得够不够」要读像素，不要靠眼睛。** 深色画面上的亮度差眼睛判不准
（歌词擦除那两半，肉眼看「好像差不多」，读回来是 252 对 84）。
把截图丢进一个 canvas 页、扫一条带里每列的最亮值、`--dump-dom` 取回来，
几行代码的事。

服务起在 `--directory` 上，**不要 `cd out`** —— 否则 `npm run build` 会因为
`EBUSY: rmdir 'out'` 失败（Windows 会把占用它的 python 进程锁住那个目录）。

### 但曲库的 CORS 只放行线上域名

上面那套环境里**列表永远是空的**：worker 回的是
`Access-Control-Allow-Origin: https://ianyspace.github.io`，`127.0.0.1` 读不到。
后果是所有依赖 `current` 的分支全部短路 —— 空列表、没有歌在放、没有封面、
没有歌词。**「构建绿 + 截图正常」在这个状态下什么也证明不了**，
线上点一首歌就崩的那个 bug 就是这么漏过去的。

要真的走一遍数据路径，用 CDP 驱动一个真浏览器，并给一个临时 profile
关掉同源策略：

```bash
# 线上：直接跑，什么都别加。两棵树全跑，跑完看 summary
node scripts/drive-page.js --all --track=夜曲

# 本地：必须 --insecure，否则列表是空的（见上）
node scripts/drive-page.js --all --insecure --base=http://127.0.0.1:8899/music --track=夜曲

# 单个页面 / 换窗口尺寸（歌词平面的机位跟比例走，窄窗口要单独扫）
node scripts/drive-page.js http://127.0.0.1:8899/music/h5/ --insecure --track=夜曲 --size=390x844
```

`--insecure` 加的是 `--disable-web-security`，只在那个一次性 profile 里生效。
`--track=X` 点包含 X 的那一行而不是第一行 —— 值得用：**只有一部分歌有歌词**，
随便点一首多半走不到歌词那条路径。`--out=前缀` 会在每步存一张截图。

它断言七件事，按页面在 `PAGES` 表里配：不自动播放、列表真的到了、点一行真的在播、
快捷键真的做了它说的事、两个开关真的翻转了、行抽屉里的动作真的生效、没有任何异常。
列表和播放都是**轮询**等而不是睡固定秒数，并把等到用了多久打出来 ——
于是「慢」和「坏」是两个答案。
**两棵树都只绑了 `Escape`**（关抽屉）；没有全局播放快捷键。

`pin`（置顶 / 取消置顶）**两套布局都跑**，因为「只接一套布局」这个缝漏过三次；
它按**整个列表的顺序**判断取消置顶，不是看第一行 —— 见上面那一段。

h5 另外挂了两段自己的：

- `shell` —— **手机端的外壳结构**：⋮ 还在、抽屉里是 `音乐库 / 缓存管理 / 切换外观`、
  `音乐库` / `账号` / `听歌排行` 是**抽屉页**（`[role="dialog"]`，**没有 `<header>`**、
  有「收起」按钮）、抽屉在被打开另一个面板时自己退场；然后是**没号码的喜欢只在本机**
  （落在 `guest` 桶、outbox 是空的），**确认号码之后它跟着号码走** —— 这一条读的是
  「数据同步」那一行自己说的话（`本地还有 N 条数据没上传`），因为那正是访客能看到的证据。
  「没有 `<header>`」这条是**结构性**的：抽屉页长回一个页面标题栏，截图上看不出区别。
  另外两条也是结构性的，各管一个刚被改掉的形状：**号码那张卡和确认块互斥**
  （`sheetState` 多读一个 `form` 布尔：没号时 `form=true` 且没有「已确认」，确认后反过来，
  按「清除」又翻回去），以及**听歌排行关掉之后屏幕上不剩任何面板** ——
  它曾经是「排行页 ⟷ 账号页」互相指向的死循环，而这一条就是那个循环的回归测试。
- `like` —— 从行抽屉喜欢 → 只看喜欢只剩那一首 → **`Page.reload` 之后还在** →
  播放页那颗心点亮并能取消 → 空的我喜欢有自己的文案 → Drive 曲库下整套入口消失。
  **这是唯一会重载页面的一段，所以它放在最后**（`shell` 那段也重载一次，
  但那是它自己清理完之后，为了把页面还给 `like`）。

> **`like` 曾经是坏的，现在修好了。** 喜欢搬去 D1 之后整条路径要先绑一个 QQ 号，
> 它既没绑号也没打桩 `/likes`，所以第一步就落空。**修好它的正是「没号码也能喜欢」这件事本身** ——
> guest 桶让这条路径不再需要号码。而 `/likes` 现在是**必须**打桩的：那个接口写的是真的 D1，
> 跑一次测试就会往里面塞一行没人拥有的号码。见下面的 `failLikes`。

`stubDriveFiles` 把 Drive 的 `files.list` 在网络层打桩（CDP `Fetch`），那是唯一能真正走到
Drive 曲库的办法 —— 列表来自一次真实 API 调用，而每来源的列表缓存只写不读。

`failLikes` 用同一套手法把 `/likes` 全部答成 503，两件事一起办：**测试绝不写生产库**，
而且**「队列排不出去」才是值得测的状态** —— 一条能立刻排空的队列，和一条空的队列，
在界面上说的是同一句话。`shell` 那段的最后一条断言就是「这个请求被拦下来了」，计数打出来。

四处写法上的坑，都是这个脚本自己踩出来的：

- **断言别挂在会变的文案上。** desktop 的歌词按钮没有 `aria-label`，而且打开后文案是
  「收起歌词」不是「隐藏歌词」，所以拿「显示歌词」这个单态选择器去复查会**匹配不到任何东西**，
  点击变成空操作、检查变成在量空气。选择器一律写**两个文案的并集**，状态去读
  `aria-pressed` / `aria-expanded`。
- **同理，别用子串判断文案。** 拿 `includes('喜欢')` 去查抽屉里有没有喜欢项，问的是一个
  更宽的问题：任何**包含**这两个字的标签都会让它通过，而它要回答的是「访客能不能从这里
  喜欢这首歌」。标签一律取回来当数组、整体比较。
- **不是每个开关都能断言一个来回。** desktop 的 `aria-expanded` 报的是
  `listOpen && !autoHidden`，是面板的**可见性**而不是用户的选择 —— 播放三秒后面板自己会折，
  于是两次点击里第一次落在已经折好的面板上、什么都看不出来。这种就配 `cycle: false`，
  只断言「点了不是没反应」。而且它**读得比另一种早**（1.2s 而不是 2.5s），并且读在截图**之前**：
  CDP 截图是一趟往返，耗时取决于机器忙不忙 —— 截图夹在点击和读取之间时，这条检查
  **单独跑过、放进 `--all` 就失败**，在一个已经做对了的页面上读到 3.0s+ 的 false。
- **快捷键要连期望一起写。** 早期版本按了空格（暂停）然后断言「还在播」，这个断言
  按构造就不可能成立。`keys` 的每一项写成 `{ key, paused }`。
- **等一个状态切换时，别等「有行数了」。** 切到 Drive 曲库那一段一开始等 `rows > 0`，
  结果它**单独跑过、放进 `--all` 就失败**：挂载时公共曲库先从永久列表缓存里画出来，
  `rows > 0` 早就成立了 —— 等它等于在等要离开的那个状态。改成等**标题里的曲库名**
  （「Music Space」→「Google Drive」），因为那才是切换本身。凡是「A 换成 B」的断言，
  等的必须是 B 的特征，不是「有东西了」。
- **两个装载可能同时在飞，所以 `loadTracks` 有 `loadSeqRef`（2026-09-21 补的）。**
  恢复 Drive 会话时**必然**有两个在飞：`librarySource` 的初值是 `cloud`，把它切成 `drive`
  的是那个读保存 token 的 effect —— 于是公共曲库先被请求、Drive 随后被请求，
  **谁最后返回谁就是屏幕上的列表**。这就是「公共曲库偶尔盖在 Drive 列表上面」的成因
  （上面那条 `rows > 0` 的坑是它的另一个侧面），而且被盖掉的不只是屏幕：
  `writeListCache` 会把那份迟到的公共列表写进**列表缓存**，于是它比标签页活得久。
  现在被取代的那次装载什么都不说 —— 列表、缓存、转圈都不动，转圈只在**最新**那次结束时收。
  冒烟里那条断言也不再靠等：`cloudDelayMs` 把公共曲库的响应**扣住 4 秒**，让它在 Drive
  列表落地时还在飞。去掉守卫重跑，它如实报 `63 rows in 30.2s`。

脚本打印每一步的 DOM 状态、所有 4xx/5xx 的真实 URL、以及**全部
`console.error` 和未捕获异常**，异常是这套东西最值钱的产出：它给出压缩后的堆栈，
对着 chunk 的字节偏移就能翻回源码那一行 —— 上面那个点歌崩页的 bug 就是这么定位到
`ex` = `ThreeHud` 的。

两个坑：**URL 要放在 Chrome 命令行上，不要用 `Page.navigate`**（驱动空白页有竞态，
第一次探测会打在 `about:blank` 上，看到 `title: ""` 和空 DOM，然后误判成「页面是坏的」）；
**别用 `--screenshot` 代替它**，截图看不出「点了会不会崩」。

> 本仓库的工作区是 **CRLF**、CI 是 **LF**，且 `core.autocrlf=true`（仓库内一律 LF）。
> 用脚本批量改源码时注意别把文件写成混合行尾（Node 里 `split('\n')` 会留下 `\r`）。

## 部署

推 `master` 触发 `.github/workflows/deploy.yml`：`npm ci` → `npm run build` → 校验产物 →
发布 `out/` 到 Pages。

**`paths-ignore` 现在有了 —— 而它的前提是「Source 仍是 `GitHub Actions`」，动它之前先读这一段。**
这个列表被加过两次、又撤过两次。撤销的理由是：Source 留在「Deploy from a branch」时，那条
legacy Jekyll 构建**每次推送都会跑**，而唯一能盖过它的是本工作流里的
「Wait out GitHub's legacy Jekyll build」那一步 —— 那一步在这个工作流**里面**。工作流被跳过，
Jekyll 构建就无人对抗，于是它成了最后一次部署，整站变成仓库根的渲染结果。

- 2026-09-21 真的发生过：一条只改 `scripts/drive-page.js` 的推送之后 `/h5/` 404、
  `README.md` 200、`.nojekyll` 404，挂了约五分钟。
- **2026-09-21 用户把 Source 改成了 `GitHub Actions`**，那条 legacy 构建从此不再被创建。
- **同一天晚些时候，这个条件被查实了，列表才重新加上**：`event: dynamic` 的 run 最后一次
  出现在 `810739a`（就是改设置之前的那次推送），之后连着七次推送一次都没有。
- 列表内容只有三条，每一条都**不可能改变 `out/`**：`**/*.md`（文档）、`scripts/**`（冒烟
  脚本，构建不 import 它）、`cloudflare-worker/**`（部署到别处）。**`config/**` 和 `public/**`
  故意不在里面** —— 前者被 `next.config.js` import，后者原样拷进 `out/`。
- 它只跳过「**全部**改动都匹配」的推送：同时改了文档和产品代码的推送照常部署。
- **设置一旦被改回分支模式，先删这个列表再推。** 否则重演上面那次事故，而且是**静默**重演：
  跳过的那次推送在 Actions 列表里什么都不显示，只有线上会变。判断设置还在不在的方法见本节
  「判断它有没有真的生效」。

**推完必须验一句「线上到底在服务什么」**，四条一起看：

```
curl -s -o /dev/null -w '%{http_code}\n' https://ianyspace.github.io/music/h5/        # 200
curl -s -o /dev/null -w '%{http_code}\n' https://ianyspace.github.io/music/README.md  # 404
curl -s -o /dev/null -w '%{http_code}\n' https://ianyspace.github.io/music/.nojekyll  # 200
curl -s -o /dev/null -w '%{http_code}\n' https://ianyspace.github.io/music/sw.js      # 200
```

最后一条查的是 `public/` 里那种**不带内容哈希**的文件：它上没上线，构建日志看不出来。

`h5` 200 只说明**那一刻**是对的（这个故障是间歇性的）；`README.md` 404 + `.nojekyll` 200
才说明当前服务的是 `out/`。要确认某个改动真的上线了，去 grep 产物里的字符串
（比如 `retryAt`）—— **`_next/static/chunks/pages/h5-*.js` 的文件名不是内容哈希**，
两次构建可能同名不同内容，所以「文件名对得上」什么也证明不了。

`npm run build` 产出 `out/`，并把 `public/**`（图标那几张）原样拷进去；工作流再补一个 `out/.nojekyll`
（否则 Pages 的 Jekyll 会丢掉 `_next/` 这类下划线开头的目录）。

**产物形状（容易记错）**：`trailingSlash: true` 时 Next 给每个路由生成一个**目录 + index.html**，
所以是 `out/h5/index.html`、`out/desktop/index.html`，**不是** `out/h5.html`。
来源见 `next/dist/export/index.js` 里按 `subFolders` 拼 `htmlDest` 的那几行。
Pages 会把 `/music/h5/` 解析到该文件，并把裸 `/music/h5` 301 到带斜杠形式。

工作流里的 `Verify build output` 会断言这些文件都在；改路由或改 `trailingSlash` 时记得同步它，
**往 `public/` 里加文件时也要**（那些文件不走 `_next/`，少一个就是浏览器里的静默 404），
否则 CI 会先于线上报错（这是有意的——少一个文件就是半个死站）。

**Pages 的 Source 必须是 `GitHub Actions`**：Settings → Pages → Build and deployment →
Source 选 `GitHub Actions`。**不要**留在「Deploy from a branch」。

如果留在分支模式，GitHub 会额外跑一个 Jekyll 构建（`pages build and deployment`，event=`dynamic`），
它会发布**仓库根目录**而不是我们的 `out/`，两者竞争导致线上**时好时坏**：
有时是我们的 app，过一会儿又变回 Jekyll 渲染的 README，
`/h5/`、`/desktop/`、`/.nojekyll` 全 404。
**不要因为「刚 push 完是好的」就以为没问题 —— 这个故障是间歇性的。**
（这个坑真实发生过：`f7a7cc1` 推完后整站就是挂的，`/` 是 README，`/h5/` 404。）

**一次性定性检查**（比看工作流状态更直接）—— 请求仓库根目录的文件：

```bash
curl -o /dev/null -w "%{http_code}\n" https://ianyspace.github.io/music/README.md     # 200 → 实锤
curl -o /dev/null -w "%{http_code}\n" https://ianyspace.github.io/music/package.json  # 200
curl -o /dev/null -w "%{http_code}\n" https://ianyspace.github.io/music/h5/           # 404 → 实锤
```

根目录文件能访问、而 `/h5/` 是 404，就说明发布的是仓库根（经 Jekyll），不是 `out/`。
另一个信号：首页源码里有 `Jekyll SEO tag` 或 `/assets/css/style.css?v=<sha>`。

排查时注意：`api.github.com/repos/<user>/<repo>/pages` 匿名访问返回 404 是**没权限**，不代表 Pages 没开。

**这个设置只能人来点，2026-09-21 用户已经点过了：Source 现在是 `GitHub Actions`。**

判断它有没有真的生效，不看设置页面，看**推送的 SHA 还有没有 `event: dynamic` 的 run**：

```bash
curl -s "https://api.github.com/repos/ianyspace/music/actions/runs?head_sha=<full sha>&per_page=100" \
  | python -c "import sys,json;[print(r['name'],r['event']) for r in json.load(sys.stdin)['workflow_runs']]"
```

没有 `pages build and deployment` 就是生效了（`head_sha` 必须传完整 40 位，短 sha 会被静默当成
「没有这个 run」）。生效之后 `deploy.yml` 里那步「Wait out GitHub's legacy Jekyll build」
**保留** —— 万一日后设置被改回分支模式，它是唯一挡在事故前面的东西 —— 但它**不是**免费的空操作：
2026-09-21 第一次在新设置下跑，它对着「查不到」一路 `sleep 20`，白等满了 5 分钟上限。
现在 `absent` 只重复看三次（约 40 秒）就收工，`running` 仍然等到 `done` 或 300 秒上限。
（教训：**「空操作」这种话要跑一遍再说** —— 那一步的注释原来就写着「no-op once the setting
is right」，而它其实是五分钟。）

**别想着从 CI 改这个设置。** 同一天试过：加一个工作流，用 `GITHUB_TOKEN` 加
`permissions: pages: write` 调 `PUT /repos/{owner}/{repo}/pages`，GitHub 直接回 **403** ——
改 Pages 站点设置要的是**仓库管理权限**，而 Actions 的 `GITHUB_TOKEN` 的权限清单里根本没有
这一项（`pages: write` 是给部署用的），换什么 `permissions:` 都不行。那条实验工作流已经删掉。

（那次实验顺手验证了一件有用的事：**job 日志要登录才能读**（匿名请求 `/actions/jobs/<id>/logs`
返回 403），**commit status 不用** —— `curl -s https://api.github.com/repos/<user>/<repo>/commits/<sha>/statuses`
就能读到每条状态的 `context` / `state` / `description`。以后凡是「CI 里跑完了但结果在这台机器上
看不到」的事，让那一步自己 POST 一个 commit status 出来，比翻日志省事得多。）
