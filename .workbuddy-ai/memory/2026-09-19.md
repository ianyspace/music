# 2026-09-19

## 规划：H5 / PC 解耦 + PC 全毛玻璃改造（未开工，方案已定稿）

方案文件：`C:\Users\aaaaa\.workbuddy-ai\plans\radiant-beacon-newton-NdXVe_d2.md`

需求：① `/h5` 与 `/desktop` 完全独立、互不引用；② PC 端样式全部改毛玻璃；③ H5 逻辑与样式不变。

关键调研结论（本次确认，后续直接可用）：

- 现状是**同一个 `MusicApp`（1924 行）**，`pages/h5.js` / `pages/desktop.js` 只差一个 `variant` prop；`variant` 真正起作用只有 3 处（`MusicApp.js:1013` 歌词默认值、`:1485` H5 氛围光斑、`:1493` 布局总开关）。
- 耦合只有 4 类：`MusicApp.js:70` 静态 import `DesktopMusic`；桌面通过回调反向打开挂在 `MusicApp` 外壳的浮层；外壳 `.page`（`MusicApp.module.scss:7-76`）同时包住两套 DOM 并提供整套 token；`variant` 的 3 个判断点。
- 桌面**已经在用毛玻璃**：`@ybouane/liquidglass`（WebGL）4 个面 + 3 处纯 CSS `backdrop-filter`（`.panel`/`.menu`/`.locate-btn`）。桌面 `.root`（`DesktopMusic.module.scss:28-93`）token 自包含，不引用 H5 token。
- **CSS Module 类名是 `[basename]__[local]__[hash]`，不含目录**（实测构建产物：`DesktopMusic_root__Kk33x`）。所以把组件挪进子目录不会改类名，5 个 `preview-*.js` 无需改动 —— 前提是**保留 basename**。
- `check-css-modules.js` 的 walk 是递归的；但 `check-covers.js:232` 与 `check-settings-persistence.js:40` 的 `readdirSync` **不是递归的**，一旦把组件移进子目录，这两个守卫会静默失效。
- 三个面板（CacheManager / DislikedSheet / DriveSheet）共用同一套外壳 markup（`veil > page > topbar > 内容`），外壳 8 个类名靠 `composes: … from './Sheet.module.scss'` 取得，内容类名各自本地定义。

用户已定的 5 个决策：

1. 抽**中性共享核心** `components/Music/core/`（状态机 + 数据层 + 纯工具），两端各自 import core，两端之间零引用。
2. 毛玻璃用**纯 CSS `backdrop-filter`**，移除 `@ybouane/liquidglass`。
3. PC 端**只换视觉皮**，保留现有桌面布局结构。
4. 面板按**拆分内容与外壳**：外壳各自实现（H5 逐字节、桌面玻璃版），内容组件 `core/CacheContent` / `core/DislikedContent` 两端复用。接受 H5 面板 DOM 类名前缀变更（CSS 规则逐字节拷贝，渲染应一致）。
5. **删除全部 11 个 `scripts/check-*.js`**（含 `deploy.yml` 里的调用行与 `AGENTS.md` 的「检查脚本」章节），只保留 5 个 `preview-*.js`。`deploy.yml` 里内联的 `test -f out/...` 产物断言保留。

⚠️ 决策 5 的后果：需求 ①③ 从此**没有机器守卫**，改动正确性靠人工逐屏比对。`AGENTS.md` 里要写明这一点。

下一步：按方案 Step 0 → Step 5 实施（Step 1 可独立提交且不影响 H5）。

---

## 实施完成并推送（commit `d2cd9fa`，已 push 到 master）

三个需求全部落地。54 文件变更，+2809 / -5019。

**最终目录**

```
components/Music/
  core/     usePlayer.js（原 MusicApp 111-1455 提升而来）、PageHead.js、PlayerAudio.js
            CacheContent.{js,module.scss}、DislikedContent.{js,module.scss}、sheetBase.module.scss
  h5/       MusicApp.js（外壳）、TrackList / NowPlaying / MiniPlayer / Profile
            CacheManager / DislikedSheet / DriveSheet（薄壳）、SheetChrome.{js,module.scss}
  desktop/  DesktopApp.js（外壳 + token 根）、DesktopMusic.{js,module.scss}
            DesktopSheetChrome.{js,module.scss}、DesktopCachePanel.js、DesktopDislikedPanel.js
  根        Cover / Marquee / icons / shared / audioCache / librarySource（共享件）
```

**验证方式（`build` 通过之外做的）**

- 把 HEAD 里的旧 `MusicApp.js` 抠出 hook 体（111-1455）与新 `usePlayer` 体 `diff`，
  差异**只有**预期的几处：H5 外壳态（tab/playerOpen/menuOpen）搬走、`clearPinnedOrder` 删除、
  三个 sheet opener 不再 `closeMenu()`、`variant === 'desktop'` → `lyricsAutoOpen`、
  末尾新增 `seek`/`toggleLyrics`/5 个 audio handler。其余逐字相同。
- 手机端分支（旧 1557-1919 去缩进 8 格）与新 `h5/MusicApp.js` `diff`，差异只有
  `tracks.length`→`libraryCount`、`rowMenu ? rowMenu.id : ''`→`rowMenuId`、
  `sourceLabel(librarySource)`→`sourceName`，以及内联 `onSeek`/`onToggleLyrics` 换成提升版。
- 写了个一次性脚本核对所有 `styles.x` 是否在同名 `module.scss`（含 `composes` 一跳）里有定义，
  跑完删掉。3 个报错全是脚本正则的假阳性（`.page.theme-dark`、`.panel:not(.panel-open)`），
  已在构建产物里确认 `DesktopApp_theme-dark__oBXQ1`、`DesktopMusic_panel-open__XskjB` 存在。
- 产物 CSS 已按页拆成两份（desktop `6f0a3a55e344506e.css` / h5 `7e98e412550283fb.css`），
  H5 那份不含任何 `Desktop*` 类名 —— 两棵树在 CSS 层面也彻底分开了。

**踩到的两个坑（重要）**

1. `git rm -f` 那次被 SIGTERM 打断，**把整个 `components/Music/h5/` 目录连未跟踪文件一起删了**
   （含新建的 `MusicApp.js`、`SheetChrome.*`），并留下 `.git/index.lock`。
   恢复办法：`rm -f .git/index.lock` → `git restore --worktree -- <paths>`。
   **注意索引里是 `git mv` 当时的内容**，所以恢复出来的 h5 文件 import 还是 `./icons` 老路径，
   要重跑一遍 `_migrate-imports.js` 那套 `./x → ../x`（只对 icons/shared/Cover/Marquee/
   librarySource/audioCache 六个共享模块，同目录兄弟组件不能改）。
   教训：`git rm` 前后别让命令被打断；删文件优先用 `rm` + `git add -A`，别用 `git rm -f`。
2. `usePlayer` 返回键名原来叫 `onAudioEnded`/`onAudioPlay`…，但 `PlayerAudio` 收的是
   `onEnded`/`onPlay`…。已把返回键**改成和 `PlayerAudio` 的 prop 同名**，
   否则外壳里手写一遍映射，写错一个就是「handler 静默不触发」。

**给未来的自己**

- 需求 ①③ 现在**没有机器守卫**（11 个 check 脚本按用户要求全删）。
  改完两套布局请自己 `grep -rn "desktop" components/Music/h5/` 之类过一遍。
- 桌面 token 只在 `desktop/DesktopApp.module.scss` 的 `.page` / `.page.theme-dark` 声明一次，
  `DesktopMusic.module.scss` 只消费不声明。`@supports not (…)` 回退块必须和声明同文件，
  且里面要写 `.page.theme-dark`（写成 `.theme-dark` 会被 `.page` 的浅色值按源码顺序盖掉）。
- `patch-package` 在 devDependencies 里但仓库没有 `patches/` 目录，是历史残留，可删。

