# PRD：PC 端沉浸式播放页重做

| 版本 | 日期 | 状态 | 变更 |
|---|---|---|---|
| v0.1 草案 | 2026-09-26 | 已否决 | 初版：双模式对等、保留旧页面 |
| v0.2 草案 | 2026-09-26 | 已评审 | 星云为默认、旧 PC 功能全部退役、播放栏常驻、3D 歌单左侧可收起 |
| v1.0 定稿 | 2026-09-26 | 已实现，观感未达标 | 评审意见全部落实：不保留其他歌单切换、预设背景 1 个、自定义仅 URL、粒子源只用封面、旧功能 M1 直接退役；新增歌词节奏联动与全局过渡动画规范 |
| v1.1 修订 | 2026-09-26 | 已评审 | 对照参考图复盘 v1.0 实现，纠正三处偏差：① 星云=单封面大画像（可辨认）；② 歌词大字嵌入粒子层之间；③ 确认歌单维持左侧+当前歌单（评审决策覆盖图的右侧形态） |
| v1.2 修订 | 2026-09-26 | **待评审** | 细节澄清：① 无"歌单"概念层，页面直接是歌曲列表，3D 卡片化（左侧、可自动收起不变）；② 歌词改**单行居中**，去掉逐字扫过与滚轮翻看，保留节奏联动；③ 星云鼠标视差明确定义为"虚拟摄像机微转" |

---

## 1. 背景与目标

### 1.1 决策变更（相对 v0.1）

用户已拍板：

1. **默认星云**：星云模式是产品的默认形态，自定义背景是可选项，不是对等双模式。
2. **旧 PC 功能全部退役**：不做「沉浸模式开关」，重做后的页面**就是** PC 端页面。现有三栏工作区、左侧完整歌单列表、顶部栏、右上角立方体设置入口等一并移除，不做兼容保留。
3. **底部播放栏常驻**：不自动隐藏，永远可见。
4. **歌单在左侧，3D 呈现，可自动收起**：对齐参考图 A 的左侧悬浮卡形态，收起后留一个唤出把手。

### 1.2 目标形态

对齐两张参考图：

- **参考图 A（星云 · 默认）**：全屏 3D 粒子星云（封面粒子化，占满整屏、有景深），UI 全部悬浮为玻璃卡片——左侧 3D 歌单、底部玻璃播放条、歌词居中。
- **参考图 B（自定义背景 · 可选）**：全屏视频/动图/图片背景，歌词居中大字，底部同款播放条。

### 1.3 非目标

- 不改移动端（h5）任何行为。
- 不改播放内核（`usePlayer` / `PlayerAudio`）、曲库来源、QQ 登录逻辑。
- 不做歌词翻译/罗马音等新数据能力（仅升级呈现）。

---

## 2. 页面整体结构（重做后，即 PC 端唯一形态）

```
┌───────────────────────────────────────────────────┐
│  L1 背景层   WebGL 全屏 canvas（星云 / 自定义背景）   │
│                                                   │
│  ┌────────┐        ┌──────────────┐               │
│  │ 歌单面板 │        │   歌词（DOM） │               │
│  │ (左侧,  │        │   居中大字    │               │
│  │  可收起) │        └──────────────┘               │
│  └────────┘                                       │
│  ▸ (收起后的唤出把手)                               │
│        ┌───────────── 底部播放栏（常驻）─────────┐   │
│        └──────────────────────────────────────┘   │
└───────────────────────────────────────────────────┘
```

| 层 | 实现 | 说明 |
|---|---|---|
| L1 背景层 | three.js（动态加载），全屏 canvas | 默认 `NebulaProvider`；可选 `MediaProvider`（视频/图/动图） |
| L2 歌词层 | DOM（CSS 3D + 滤镜） | 保留可选中、可读屏 |
| L3 悬浮 UI | DOM（毛玻璃） | 左侧歌单面板、底部播放栏、设置弹层 |

---

## 3. 默认模式：星云（对标参考图 A）

### 3.1 视觉（v1.1 重写：单封面大画像，对标参考图 A）

**核心观感标准：当前歌曲的封面被放大到近乎撑满视口，粒子化之后轮廓依然清晰可辨**（参考图 A 里 The Weeknd 的人像占满整屏）。v1.0 实现的"封面点云 + 星尘星野"观感不达标，按以下规格重做：

- **画像主体**：只采样当前封面，画面占视口高的约 1.2~1.4 倍（略微溢出屏幕，相机贴近），采样密度 ≥15 万粒子、点径约 1 个格距，按封面亮度/对比度加权采样保住高对比区域，保证人像轮廓与五官可辨。
- **景深**：粒子沿 Z 轴分前中后三层微抖，前景少量大尺寸粒子自然虚化，形成纵深；雾气感由远景粒子密度衰减产生。
- **星尘**：从 1.4 万降到 ≤5000，只做边缘氛围陪衬，不再与画像抢画面。
- **底色**：近黑底 + 画面四周渐晕，突出画像。
- **虚拟摄像机（鼠标视差，v1.2 明确）**：鼠标移动带动一个虚拟摄像机绕星云主体做小角度偏转（俯仰/偏航各几度），星云本体几何不变，只有投影视角变化——用户由此感知到星云是立体的。移动平滑带惯性，停止后缓慢回中。
- 音频联动（保留 v1.0 链路）：
  - 低频：画像整体呼吸膨胀/收缩（幅度以不破坏轮廓为限）；
  - 高频：局部星点闪烁；
  - 节拍：冲击波沿 Z 轴推进（粒子短暂外扩后归位）。
- 暂停：缓慢漂浮（呼吸态）；切歌：散开重组。

### 3.2 首次进入

- 页面加载即进入星云模式（无需任何开关操作）；设置里可切到自定义背景，切回同样一步。

### 3.3 粒子源（已拍板）

- 首版**只用当前歌曲封面**，不做艺人照片等新图源。

---

## 4. 可选模式：自定义背景（对标参考图 B）

### 4.1 背景源（已拍板）

- **预设库**：首版只内置 **1 个**简单预设（一段免版权氛围视频，R2 托管），验证链路后视需要再扩。
- **自定义**：粘贴视频/图片直链 URL 存入「我的背景」（localStorage），可删除；**不做本地上传**。
- 统一抽象为 `BackgroundProvider`（nebula / video / image / gif），切换交叉淡入无白屏。

### 4.2 视频背景策略

- 循环、静音、`playsInline`，可压速 0.8×。
- 自动叠加可读性滤镜（压暗 + 渐晕，强度可调），保证白字歌词可读。
- 无音频联动，但歌词换行仍有轻量动效（§6）。
- 加载失败降级为封面模糊静态图。

---

## 5. 悬浮 UI

| 组件 | 位置 | 行为 |
|---|---|---|
| **3D 歌曲列表**（v1.2 重定义） | **左侧**，悬浮玻璃卡 | **没有"歌单"概念层**——页面进入直接呈现当前这份歌曲列表本身。每首歌一张玻璃卡：封面缩略图 + 歌名 + 歌手；整列置于 3D 透视空间，卡片远近错落形成纵深，悬停卡片浮起并倾斜；点击切歌。**不做其他歌单切换** |
| 列表自动收起 | 左侧 | 鼠标离开列表区域 5s → 滑出至屏幕左缘，留一个竖条把手（封面色渐变 + 当前播放指示条）；点击把手或鼠标移至左缘唤出；播放暂停时不收起 |
| **底部播放栏** | 底部居中，玻璃胶囊，**常驻不隐藏** | 封面缩略图、歌名/歌手、喜欢、上一首/播放/下一首、词开关、进度与时间、音量、全屏 |
| 设置入口 | 播放栏内齿轮 | 弹出面板：背景模式（星云/自定义+素材选择）、星云强度、可读性滤镜、列表自动收起开关 |
| 全屏 | 播放栏内 | Fullscreen API |

自动收起的例外规则：鼠标悬停在面板上、播放暂停、用户刚完成一次切歌（3s 内），均不收起。

---

## 6. 歌词层（v1.2 修订：单行居中，嵌入场景）

- **层次**：歌词 DOM 夹在粒子两层之间——底层 canvas 画封面画像主体，歌词 DOM 层叠其上，再叠一层稀疏的前景粒子 canvas 少量遮挡歌词，形成"歌词活在画面里、被粒子部分遮挡"的纵深观感（对标参考图）。
- **单行显示（v1.2）**：任何时刻**只显示当前一句**，居中，无上下行小字、无历史/预告行。
- 位置与字号：视口居中，字号约视口高 8~10%，加粗 + 柔投影（对标参考图 B 的单行大字形态）。
- **无逐字扫过（v1.2 拍板）**：不做卡拉OK逐字填充；歌词的"动"完全来自节奏联动。
- 换行动效：旧行上浮淡出、新行就位；星云模式同时触发一次画像冲击波，让词与画面咬合。
- **节奏联动（v1.0 已拍板，保留）**：歌词必须能"感觉到音乐"——
  - **节拍脉冲**：检测到节拍时，当前歌词行做一次轻微的缩放/亮度脉冲（幅度克制，约 1.03~1.06 倍，随强度档位），连续节拍下呈现"随鼓点呼吸"的效果；
  - **响度呼吸**：当前行文字亮度/辉光随实时响度浮动，安静段收、高潮段亮；
  - **换行对拍**：换行动效的时长贴合歌曲 BPM 感（快歌干脆、慢歌绵长），避免快歌时动画还没放完下一行就来了；
  - 实现上全部复用 `audioAnalyser` 的低频/响度采样，节拍检测与星云共用同一份检测结果，不重复计算。
- 交互：点击歌词区 = 播放/暂停。（v1.2 拍板：不做滚轮翻看，永远只显示当前句。）

---

## 6.5 全局过渡动画规范（本轮新增，已拍板）

所有过渡动画必须丝滑顺畅、因果合理，统一遵守：

1. **统一时长体系**：微交互 150–200ms、面板进出 300–400ms、场景级切换 600–800ms；同一类交互全站时长一致，不各自为政。
2. **统一缓动**：面板/浮层用 `cubic-bezier(0.32, 0.72, 0, 1)`（标准"玻璃滑入"曲线）；内容淡入淡出用 ease-out；**禁用 ease-in-out 线性感**和生硬的 linear。
3. **GPU 合成优先**：所有动画只动 `transform` / `opacity` / `filter`，禁用触发 reflow 的属性（width/height/top/left）；大量 DOM 动画加 `will-change` 并在动画结束移除。
4. **交叉淡入**：任何"消失+出现"（切歌封面、背景模式切换、面板收展）一律新旧交叉淡入，不允许白屏/闪跳。
5. **因果与方向**：动画方向表达空间关系——歌单从左缘滑入滑出、设置从播放栏弹出、歌词行向上流动；元素去哪就从哪个方向走，不凭空缩放消失。
6. **可中断**：动画进行中再次触发时从当前状态平滑接管（CSS transition 天然支持，JS 驱动的节拍脉冲需做插值衔接），不允许跳变或排队卡顿。
7. **性能红线**：动画期间掉帧即降级——节拍脉冲等非必要动效在检测到帧率低于 45fps 时自动减弱（复用强度档位思路）。

---

## 7. 旧功能处置清单（明确退役，M1 上线即替换）

| 旧功能 | 处置 |
|---|---|
| 三栏工作区（左列表/中舞台/右栏） | 移除 |
| 居中唱片 + 唱臂（Tonearm） | 移除（星云取代） |
| 现有 CoverParticles（2 万粒子平板粒子云） | 代码升级迁移进 NebulaProvider，不再单独存在 |
| 右上角立方体设置入口 + 右滑面板 | 移除，设置并入播放栏齿轮 |
| 顶部栏（搜索/账号等） | 移除；账号入口并入设置弹层 |
| 旧主题切换（亮暗色） | 沉浸页固定深色玻璃风 |
| 唱片波纹 / 歌词样式等旧视觉开关 | 移除 |

---

## 8. 设置与持久化（localStorage，前缀 `music:setting:`）

| 键 | 值 | 默认 |
|---|---|---|
| 背景模式 | `nebula` / `custom` | `nebula` |
| 星云强度 | `calm` / `standard` / `strong` | `standard` |
| 自定义背景列表 | `[{name,type,url}]` + 当前选中 | 空 |
| 可读性滤镜强度 | 0–100 | 40 |
| 列表自动收起 | on / off | on |
| 星云模式歌词显示 | on / off | on |

---

## 9. 技术方案（v1.1 修订）

1. **双层 canvas 分层（替代 v1.0 的 three.js 单 canvas 方案）**：
   - **底层 canvas**：封面大画像粒子（主体）+ 极淡星尘，现有手写 WebGL 管线升级（采样加权、密度、缩放、景深），零新依赖；
   - **前景 canvas**：仅 1500~2500 个大尺寸低透明度漂浮粒子，负责"歌词被粒子遮挡"的纵深错觉；
   - 歌词 DOM 夹在两层之间，逐字扫过/节拍脉冲等 v1.0 动效全部保留。
   - 放弃 three.js：v1.0 手写 WebGL 已验证可行，引入场景图对观感无增益还 +150KB。
2. **场景架构**：`ImmersiveApp`（舞台管理：双 canvas、rAF、资源生命周期、WebGL 上下文丢失恢复）+ Provider 体系：
   - `NebulaProvider`：双层粒子管线（画像层 + 前景遮挡层）；
   - `MediaProvider`：视频/图片背景 + 可读性滤镜（前景粒子层在此模式下同样叠加）。
3. **页面骨架**：`pages/desktop` 渲染 `ImmersiveApp`（舞台 + 歌单面板 + 播放栏 + 歌词），旧 `DesktopMusic` 已退役删除（v1.0 完成）。
4. **不动清单**：`usePlayer`、`PlayerAudio`、`audioAnalyser`、`librarySource`、曲库/歌单状态、h5 全部。
5. **性能护栏**：页面不可见暂停 rAF 与视频；粒子数随屏幕尺寸自适应；强度档位。

---

## 10. 里程碑

| 阶段 | 内容 | 可验收标志 |
|---|---|---|
| M1 骨架 | ImmersiveApp 全屏舞台 + 左侧 3D 歌单面板（含自动收起）+ 常驻播放栏 + 歌词居中；**旧 PC 页面同版本直接退役删除** | 现有粒子作过渡背景，页面已焕然一新 |
| M2 星云 2.0 | 单封面大画像重做（加权采样/密度/近景）+ 双层 canvas 歌词嵌入 + 音频联动增强 + 歌词节奏联动 | **画像可辨**，对齐参考图 A 观感 |
| M3 自定义背景 | 1 个预设 + URL 自定义 + 视频策略 + 模式切换 | 对齐参考图 B 观感 |
| M4 打磨 | 逐字歌词、换行冲击波、全屏 API、全局动画规范（§6.5）逐项过检、性能 | 全功能验收 |

每期独立提交、独立验收，可随时叫停。

---

## 11. 风险与对策

| 风险 | 对策 |
|---|---|
| 旧页面直接退役，用户习惯断层 | 播放内核与歌单逻辑不变，操作心智（点击切歌、进度拖动）全部延续 |
| 内置视频素材体积与版权 | 只用免版权素材（Pexels 等），R2 存 720p/1080p H.264/H.265 双份，按需加载 |
| three.js 体积 | 动态加载仅沉浸页；其他页面 0 影响 |
| 自定义 URL 跨域/失效 | video 标签天然容忍跨域；失败降级封面模糊图并提示 |
| 大粒子规模压力 | PC-only + 粒子数自适应 + 强度档位 + 页面隐藏暂停 |
| 歌单面板收起后可发现性 | 把手带当前播放指示条常亮；设置里可关闭自动收起 |

---

## 12. 已拍板决策记录（原待评审问题）

| 问题 | 决策 |
|---|---|
| 歌单面板是否保留其他歌单切换 | **不保留**，面板只展示当前歌单 |
| 预设背景数量与风格 | 首版只做 **1 个**简单预设 |
| 自定义背景本地上传 | **不做**，M3 仅支持粘贴 URL |
| 星云粒子源 | **只用封面** |
| 旧 PC 功能退役节奏 | **直接退役**，M1 上线即替换旧页面 |
| 歌词节奏效果 | **必须有**（§6 节奏联动：节拍脉冲/响度呼吸/换行对拍） |
| 过渡动画 | **必须丝滑**，遵守 §6.5 全局动画规范 |
| **v1.1 星云形态** | **单封面大画像**：封面放大近全屏、粒子化后轮廓可辨；废弃 v1.0 的"点云星野"观感 |
| **v1.1 歌词形态** | **大字嵌入场景**：歌词夹在粒子两层之间、字号提到视口高 8~10%、居中偏左 |
| **v1.1 歌单面板位置** | **维持左侧 + 只显示当前歌单**（评审决策覆盖参考图的右侧多歌单形态），样式升级为参考图质感的玻璃卡 |
| **v1.2 列表形态** | **无"歌单"概念层**：页面直接呈现歌曲列表本身；**3D 卡片列表**（每首歌一张玻璃卡、透视纵深、悬停浮起倾斜），左侧 + 自动收起不变 |
| **v1.2 歌词** | **单行居中**大字嵌入场景；**不做逐字扫过**；**不做滚轮翻看**；动效只来自节奏联动（节拍脉冲/响度呼吸/换行动效） |
| **v1.2 星云视差** | 鼠标视差 = **虚拟摄像机微转**：星云几何不动，仅视角小幅偏转（带惯性、回中），强化立体感 |
